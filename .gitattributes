# 仓库统一以 LF 存储并以 LF 检出。
#
# 历史上全局 core.autocrlf=true 让 git 在检出时把 LF 转成 CRLF，但本项目的
# 工具链（node、prettier、vitest 以及各类代码生成）写出的一律是 LF，于是被
# 工具重写过的文件在工作区是 LF、git 却认为应当是 CRLF，每次触碰都报
# "LF will be replaced by CRLF"。索引中 1000+ 个文本文件本来就全是 LF，
# 这里显式声明，使规则不再依赖各人的本地配置。
#
# PowerShell 5.1 解析 LF 行尾脚本没有问题；仓库内也没有 .bat/.cmd
# （只有它们才真正依赖 CRLF）。
* text=auto eol=lf

# 二进制：不做行尾或文本处理，避免被误判后损坏内容。
*.png binary
*.zip binary
*.bin binary
*.tdx binary
*.ico binary
*.woff binary
*.woff2 binary

# 定字节测试夹具：按外部格式（通达信 T0002/hq_cache、申万名单）逐字节构造，
# CRLF 和 GB18030 字节都是被断言的内容本身。上面的 eol=lf 会把它们规范化成
# LF，测试就会静默失去 CRLF 分支——`tests/fixtures/industry-members.bin` 和
# `tests/fixtures/tdx-local-blocks/*` 都曾因此长期失败（原先靠 core.autocrlf
# 在检出时回转才偶然通过）。这些路径不做任何行尾或文本处理。
tests/fixtures/tdx-local-blocks/*.dat binary
tests/fixtures/tdx-local-blocks/*.cfg binary

# Source snapshots preserve exact bytes while remaining text-diffable.
docs/trading-skills-source-snapshots/locked/** -text
docs/trading-skills-source-snapshots/unaligned-current/** -text
