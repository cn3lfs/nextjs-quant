# Fixtures are data, not code. Reformatting them changes what the tests assert:
# ths-statement.html feeds fingerprintSource assertions, and reflowed JSON
# would break the file-hash provenance R2/R2c record.
tests/fixtures/
