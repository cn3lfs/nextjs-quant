node_modules/
.next/
runtime/
desktop/
release/
.env
.env.*
!.env.example
*.tsbuildinfo
playwright-report/
test-results/
.test-data/
*.sqlite*
*.db
.playwright-cli/
.playwright-browsers/
output/
.playwright-mcp/

# 云端（SuperMind）取数冻结快照：内容寻址、体积随证券数与日期数增长，
# 是回测的输入数据而不是运行日志，因此不进 `.codex-runs/`。
.data/

# 管理者与执行者之间的派单书、运行日志与中间审计输出，不入库。
.codex-runs/
