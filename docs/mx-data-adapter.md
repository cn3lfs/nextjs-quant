# 东方财富 mx-ds-mcp 查询适配

2026-09-15：11 类工具均经实际服务和应用 API 复测，9 类返回正常，新闻与公告两类返回了超出时间范围或混入其他信息类型的数据。版本 2 增加可追踪的 scopeWarnings，并在页面显示原表与警告，不裁剪数据伪装语义通过。仅对明确日期和已知表字段核对，不能声称自然语言请求全部准确；详情见[执行记录](2026-09-15/02-data-source-execution.md)。

入口：行情页上方“东方财富 MCP 数据查询”，展开后选择类别，填写标的/范围、时间和查询内容，手动提交。使用 Codex 本地 mx-ds-mcp 配置中的官方地址及现有授权，项目不保存新的凭证、不执行配置中的命令、不展示请求头。配置可读不代表授权有效，401明确报错且不自动重试。

用户明确确认自然语言查询不接入图表。现有东方财富 HTTP K线、自动源优先级和持久化source配置保持独立；MCP不是第五个图表源，不参与RPS、历史拼接和策略计算。

## 契约

- 11个工具按明确类别路由，每次仅提交一个query字符串。不把新闻、公告、数值查询相互替代，不自动跨工具兜底；多场景/多品种由用户分开提交。
- 必须提供标的/范围、时间、查询内容；单次最多500个唯一标的，超限先拆分。工具及官方HTTPS地址限制在允许列表；仅读取必要授权字段，不启动Python代理。
- 65秒总超时，支持服务端取消，单次无自动重试。拒绝isError、非法JSON、结构异常和超大响应；data空且有message显示来源错误，空且无提示显示无数据，非空且有提示保留提示。
- 实网成功响应会省略message，内部兼容为空字符串；保留data、表格、单位、日期、来源提示及请求版本哈希。不把万/亿取整数值还原成精确行情，不推断复权。
- 标准sheetName/columns/items表格直接展示，其他data结构显示JSON。查询完成时间与原表数据日期分开；不渲染来源HTML，不执行返回内容。

实现：[契约](../src/lib/mx-data.ts)、[服务](../src/server/mx-data.ts)、[页面](../src/components/mx-data-query.tsx)。来源eastmoney/mx-ds-mcp，版本mx-data-2，API为mxDataQuery。

## 实测（2026-09-14）

先通过MCP SDK独立直连官方服务，执行tools/list和宁德时代历史行情问句，未使用新adapter。实际提供11个工具，返回成交量“2618万股”等取整数值，附带未请求指标，没有明确复权确认。因此采用独立资料展示。

adapter实网：A股工具一次查询600000、000001、300750、688981、920002；基金工具查询510300场内收盘价；指数工具分别查询上证指数、申万二级半导体指数。4次查询通过，覆盖8类样本身份及2026-09-14日期。仅证明这些资料请求有返回，不代表全市场或全部11类工具已完成实网验收，更不代表分钟线可用。

```powershell
pnpm exec vitest run tests/mx-data.test.ts tests/mx-data-transport.test.ts
$env:QUANT_MX_ACCEPTANCE = '1'
pnpm exec vitest run tests/mx-data-live.test.ts
Remove-Item Env:QUANT_MX_ACCEPTANCE
```

离线覆盖11类路由、入参边界、表格保真、空/错误/部分提示、取消、指纹、官方地址限制、配置缺失、401不重试及错误脱敏。真实验收需显式开启，测试框架自动隔离数据目录。

## 工程检查

相关9个文件57项定向测试及typecheck通过。真实接口验收分别运行adapter直调和应用API caller，均通过4次查询；不等于浏览器点击验收，当前浏览器工具没有可用浏览器。

全量首次2179通过、18跳过、4失败：3项来自新增入口的旧结构快照及原生控件约束，已核实MarketView仅增加导入和MxDataQuery节点，更新两处精确指纹并复用Select/Table，三项均已包含在57项定向复测中通过。另一项为既有workflow-scripts在20:00后固定期待MarketOnly=True，与脚本按当前时间切换False冲突；本轮未修改它。未再次运行全量，不声称全量全绿。

最终UI调整后的build（含runtime重建）通过，使用隔离QUANT_DATA_DIR，不使用生产库。改动文件格式及diff检查通过。未提交、未推送、未打包。
