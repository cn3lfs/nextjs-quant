# 本地全池选股与严格时点阶段修复（2026-09-09）

本次仅收尾严格模式失败后的阶段与失败计数，并建立本地 Git 基线；其余优化暂停，等待重新规划。

- 修复：进入日历核验前记录“核对选股基准日与交易日历”；严格时效拒绝时保存已处理 1 步、失败 1 步。拒绝仍发生在候选快照写入和自动研究之前。
- 回归测试先在修复前失败，实际阶段为“准备证券主档”；修复后通过。
- `scripts/verify-current-screen.ts` 使用隔离数据库、关闭自动 AI，只读 `E:/new_tdx64`。扫描本地日线 5,895 只，候选 616，只读错误 0，排除 608（日期落后 592、历史长度不足 16）。这不是完整市场覆盖证明。
- 候选截至 2026-09-08；616 个保存快照逐只重算一致，13 页拼接及完整导出一致，原任务记录未改变。
- 严格模式无法确认当前应有时点，正确失败；阶段与失败计数断言通过，没有新增快照、研究任务或报告。
- 本次结果文件：`output/current-screen-verification.json`、`output/current-screen-export.json`（忽略，不提交）。导出 SHA-256：`688ca3022ef012667372b4cf7506648252725e0c870d09d87cd32fd5ad64cc67`。
- 验证：`pnpm typecheck`、`pnpm test`（147 文件 / 724 测试）、`pnpm build`（包含 runtime 构建）、`pnpm desktop:prepare`、`pnpm desktop:smoke` 全部通过；冒烟确认源数据未改变。三个本次代码文件的 Prettier 检查通过。

此前目录没有 Git 历史。本次按工程基础、后端与共享模型、前端、测试与文档建立四个基线提交，最后单独提交本修复。中间基线提交是职责分组，不代表可独立运行版本；完整检查针对最终组合。没有推送远程、打包 EXE、调用真实 LLM 或发送聊天消息。
