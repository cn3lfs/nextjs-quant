# 通达信数据层优化执行记录

执行依据：[完整方案](C:/Users/jm/Documents/观澜通达信数据层优化方案-2026-09-11.md)。目标范围保持P0—P4；此文记录证据，不替代原方案验收条件。

## P0：基线与客户端下载验证（进行中）

2026-09-11 15:25完成16个样本、日线和5分钟共32项只读检查。报告：[原始JSON](C:/Users/jm/Documents/tdx-baseline-20260911-1525.json)。

- 16个样本的5分钟末根均为2022-11-30 15:00，证实分钟更新链路缺失。
- 14个可完整解析的日线样本末日均为2026-09-10。
- 上证指数日线于1991-05-09、深证成指日线于1995-03-01触发既有严格解析错误。该证据表示全历史不能整体通过校验，不表示近期数据不可读；后续核对异常记录及现有图表的在线补充路径，不放宽解析掩盖问题。
- 新增scripts/tdx-data-baseline.ts，复用应用解析器，记录源hash、起止时间、根数、目标日缺口与末根OHLCV；不写数据库或通达信目录。
- 缺口按普通完整交易日列出，未推断停牌、交易日或盘中未到时段为下载失败。
- 本次未发现通达信运行进程；桌面控制工具没有返回可用应用，浏览器连接亦失败。客户端下载、最小化、锁屏/注销及盘中落盘能力均未验收。此为当前环境限制，不推断客户端本身不支持。
- 对本机infoharbor_ex.code只读检查得到5570条有效名称、5570个唯一纯代码；包含000001和600519，但未包含600001/600002/600003/600005。不能将此文件视为历史名称的完整替代源，P1仍需保留交易所历史名录及来源冲突处理。

复现：`pnpm exec tsx scripts/tdx-data-baseline.ts --root E:/new_tdx64 --date 2026-09-11 --report C:/Users/jm/Documents/新的基线文件.json`。报告文件必须为新文件，避免覆盖已有证据。

本阶段工具验证：3项定向测试通过，typecheck通过；全量229个测试文件通过、5个跳过，1307项测试通过、6项跳过。向通达信目录写报告的负向检查被拒绝，未生成文件。此次仅增加只读工具，无应用运行逻辑或数据库变更，未打包或提交。

## 后续完整验收清单

### P1独立推进：本地名称补全（部分完成）

P0客户端下载的桌面连接验证仍待完成；按照授权继续可独立验证的名称解析，不将P0判定为通过。

- 新增GB18030补充名称解析，保留TNF优先级，来源不一致显式记录；空白/全半角差异不标成实质冲突。文件缺失或损坏不清空已有名称。
- 真实全量审计发现infoharbor的5570个代码与TNF重合，未增加本机名称覆盖；没有把接入新源当成补名成功。
- addedcode_bj.cfg提供345条历史代码关系，其中248条对应本地未匹配文件。补充名称及映射证据，保留原代码历史，不拼接目标行情，不将旧代码加进当前TNF证券池。
- [15:36审计JSON](C:/Users/jm/Documents/tdx-name-audit-20260911-1536.json)：本地6274个品种保持不变，已匹配由5896增至6144，剩余130个（沪54、深74、京2）；当前TNF池5570个不变。审计使用独立临时数据库，未写生产库。
- 剩余沪深条目多数只有旧分钟文件，包括609xxx、003xxx等，未猜测名称、未隐藏。映射覆盖、历史退市/换码及特殊品种分类仍待核实。
- 基础解析使用固定二进制fixture及非法输入测试；证券资料面板增加来源冲突与历史映射展示。
- 行业/概念源与快照、缺名后续核验、目录发现和P2—P4仍未完成。
- 本批验证：12项定向测试通过；全量231个文件通过、5个跳过，1314项测试通过、6项跳过；typecheck、build（含runtime:build）、desktop:prepare通过。桌面控制连接不可用，未完成新增资料文案的视觉验收；未打包或提交。

### P1独立推进：本地板块读取与成分快照（已实现，集成验收中）

- 新增五文件只读解析：infoharbor_block.dat、spblock.dat、tdxhy.cfg、tdxzs.cfg、tdxzs3.cfg。保留市场前缀，核对主文件声明的成员数量，跨文件读取期间变化拒绝发布。
- 本机得到269个概念、117个指数分组、467个通达信研究行业分组；目录不展示没有成分的分组。行业层级分组不是467个互斥一级行业，与现有申万分类分别显示。
- [真实审计](C:/Users/jm/Documents/tdx-block-audit-20260911-1548.json)：通达信A500主文件名单为空，通过spblock补齐500只；与D:/wsWDZ/Blocks的现有500只成分完全一致。此结果仅证明本次两个来源一致，不证明任意历史日期的成分正确。
- 行情、盘中预选、研究与CLS候选读取入口传递通达信根目录；行情可选择其他指数成分，研究仍只允许A500，新增来源选择。
- 选中通达信名单保存内容寻址快照，记录分类口径、源文件hash、观察时间及源中可得日期。未提供日期时保留null，不将文件mtime或指数建立日当成成分生效日。
- 固定二进制fixture验证全名恢复、跨市场成分、行业父子级、截断名单拒绝、源文件变化产生新快照、旧快照保留及A500来源校验。待全量回归与构建结论。
- 板块汇总RPS的原有批次仍使用既有Blocks来源，本批接入的是成分筛选读取；后续需要统一来源选择。P0/P2真实客户端验证及P3/P4仍待完成。
- 验证：全量232个文件通过、5个跳过，1317项测试通过、6项跳过；随后补充的A500来源限制测试连同相关定向用例9项通过。typecheck、build（含runtime:build）、desktop:prepare与所改文件格式检查通过。未执行桌面视觉验收、打包或提交，用户维护的mcp.ts无改动。

- P0：盘中/盘后真实下载及本地文件推进；客户端前台/最小化等场景；与终端K线对账。
- P1：本地名录/板块解析及来源冲突；缺名保留；指数不依赖本地文件发现；成分快照。
- P2：循环下载控制、锁与恢复、同日追加及末根修订、稳定读取、衍生缓存版本、全市场分批。
- P3：日线增量源实测、修订与缺口修复、三次RPS与CLS调度整合、连续3交易日验收。
- P4：研究快照复现与成分时点；基于实测决定分析存储是否必要。

当前未完成以上阶段，不将工具或单元测试通过视为客户端下载闭环完成。


### P1 板块 RPS 来源接入

新增统一来源适配器，接通手动 worker、定时观察批次和旧调度入口；行业页面可以选择 Blocks 或通达信，概念共用该选择。快照携带分类和源 hash，行业页面按结果实际分类显示。本机只读验证：一级行业30组、5577条成员关系；概念269组、46745条成员关系。5项本地板块测试及 typecheck通过，全量与构建待本轮结果。未运行实际全市场 RPS、未改生产配置，客户端下载验收仍未完成。

板块 RPS 本轮最终验证：全量232个文件通过、5个跳过；1319项通过、6项跳过。最终build（含runtime）及desktop:prepare通过，未打包、未改生产配置。

### P3独立源验证：日线增量

- 2026-09-11约16:09实测g4day/20260910.zip为HTTP200，2727375字节，内含沪深京6个COD/MD1文件；20260911.zip为404。当日未发布不能认定下载完成。
- 沪27928、深24238、京381条代码映射，不能把包内总条目数等同于A股证券数。
- 新增只读COD/MD1解析器及手工二进制fixture；校验长度、代码、重复、块偏移、价格、精度，空行情与缺失映射分别记录。未合并到生产行情。
- 真实样本sh600519、sh000001、sz000001、sz399001、bj920002均成功读取，成交量与本地2026-09-10末根一致；价格与金额差异符合.day的两位价格和float32金额精度。指数成交量保留tdx-raw单位，不擅自标股。
- 真实审计：C:/Users/jm/Documents/tdx-increment-audit-20260911-1612.json；下载文件位于独立临时目录quant-g4day-audit-ljrgw_ip，不写通达信和生产库。
- 3项定向测试及typecheck通过；当日修订合并、缓存读取接入、发布时间重试和调度替换仍未完成。客户端控制再次返回apps/browsers空及fetch失败，分钟下载继续待实测。


### P3 增量缓存发布（待行情入口接入）

- 新增SQLite独立记录类型保存不可变日线增量批次与逐证券逐日当前指针；事务发布、同日修订生成新快照，旧版本保留。较早观察/同时间冲突/批次缺失拒绝更新，重复内容复用快照。无数据库schema迁移。
- 新增 scripts/tdx-increment-cache-audit.ts，只写自动创建的隔离临时数据库。真实昨日包沪深京5样本发布和回读一致，报告C:/Users/jm/AppData/Local/Temp/quant-increment-cache-audit-0h4j3C/increment-cache-audit.json。
- 原解析器全量回归233个文件通过、5个跳过；1322项通过、6项跳过。新增缓存与解析器相关4项测试通过，typecheck通过。缓存未接图表/研究/定时下载，不能将此验收当成数据更新交付。


### P3 图表读取增量缓存

- 日线图表在周期合成前读取对应证券/日期的已发布增量，按日期修订或追加；日/周/月沿用同一聚合逻辑。只接本地未复权日线，不混入在线或分钟来源，带historicalAsOf的快照不应用当前修订。
- 图表结果标识tdx-local+g4day及原始单位，保存增量版本引用；衍生hash绑定基础版本。原始行情及历史研究对象保持不变。没有增量时继续原链路，缓存异常保留原本地数据并进入既有在线补充逻辑。
- 图表实际响应、同日修订、原快照保留、来源限制和周期合成共11项定向测试通过；构建与全量回归正在运行。自动下载/全量覆盖/研究及RPS增量入口尚未接入。


### P3 自动下载与A500隔离闭环

- 新增 downloadDailyIncrement：60秒超时、16MB流式上限、拒绝非ZIP和跳转，404返回not-published供调度重试。
- Windows内置ZipArchive解压仅允许目标日期沪深京六个COD/MD1文件；拒绝路径穿越、重复/额外/错日期成员，限制单文件及总解压量，临时目录用后清理。未新增依赖。
- refreshDailyIncrement连通下载、解压与跨市场事务发布，后一个市场缺失会回滚整个批次。
- 真实5样本下载发布报告：C:/Users/jm/AppData/Local/Temp/quant-increment-refresh-audit-hkNgnI/refresh-audit.json。
- 真实A500昨日包闭环报告：C:/Users/jm/AppData/Local/Temp/quant-increment-refresh-audit-CM378H/refresh-audit.json；沪260、深240，共500只，缺失0。数据均在独立临时数据库，生产配置与通达信目录未修改。
- 下载/解压4项、跨市场回滚1项测试通过，typecheck通过。上一批全量235文件通过、5跳过，1325项通过、6跳过；本批新全量仍运行。
- 尚未切换Windows定时任务、接入RPS/研究的增量读取，也未完成分钟客户端下载或连续三交易日验收。


### P3 持久任务与无窗口入口

- runIncrementJob保存running/waiting-publication/published/failed、清单、尝试次数、下次重试时间和成功版本引用；同日期共享跨进程租约，发布前校验租约。一次调用只尝试一次，15分钟后可重试，自动上限8次，显式force可继续。成功快照不会因后续未发布/失败被清空。
- workflow-runner新增 --increment-a500 [YYYY-MM-DD] [--force]；未成功退出码2，不冒充收盘全市场RPS就绪。当前仅提供A500重点池入口，尚未注册或替换生产Windows任务。
- 真实编译runtime隔离运行已通过：2026-09-10，count500，published，attempts1。使用先前CM378H隔离库，仅在该库启用工作流，没有修改生产库。
- 任务等待/节流/失败重试/上限/强制重试/单实例锁测试通过；构建、desktop:prepare通过，最后入口微调后runtime重建与typecheck通过。前一轮全量237文件通过、5跳过，1330项通过、6跳过。
- RPS、研究增量读取、全市场下载与修复、生产调度和P0/P2客户端验收仍待完成。


### P3 沪深全市场覆盖与部分发布

- 扩大验证发现MD1存在开高低0、量额0、close保留旧价记录。解析为no-trading-vendor-record，不推断停牌、不伪造K线；全0为empty-vendor-record，缺代码为absent-code。
- 新增显式allowUnavailable部分发布模式，严格模式保持缺失拒绝。仅有效记录更新逐股指针，无交易/缺失记录保留原有效值和审计。刷新及任务状态区分partial与published，partial退出码2。
- 无窗口入口 --increment-hs 根据当前沪深TNF A股名录运行，未删除未知/缺数据证券。真实2026-09-10包：请求5224，有效5206，缺失18（6无交易、11全0、1缺映射sz001246）。报告C:/Users/jm/AppData/Local/Temp/quant-increment-refresh-audit-CM378H/hs-refresh-audit.json。只写隔离库。
- 8项定向测试、typecheck与runtime重建通过。上一批全量238文件通过、5跳过；1332项通过、6跳过。尚未核实18个品种对应停牌/上市日期，也未修改生产任务。


### P3 RPS增量读取与来源

- 手动RPS依赖及午/尾/收盘观察共用增量叠加后的本地未复权日线，再进入既有后复权计算；GBBQ校验和90%新价覆盖门槛保持。
- 本地上证指数参考日期合并已发布增量日期，不凭系统日期追加交易日。沪深下载入口附带7个常用指数，确保参考指数可同步更新；指数不加入个股RPS排名。
- RPS结果source.incrementSnapshots记录实际使用的增量版本，重跑已有结果不覆盖原版本。
- 增量读取、参考日历及RPS/工作流15项定向通过；随后追加落库版本保留断言，相关9项通过。构建和前一批全量仍在运行。生产任务尚未切换，未宣称已完成当日真实收盘排名。


### P4 研究采集增量与不可变数据集

- 新建研究数据集在采集阶段读取日线增量，个股与基准分别保存实际来源及版本引用，内容参与dataset hash；已有已保存数据集不随当前缓存修订。研究基准读取增加文件大小/ctime/inode一致性核对。
- 快照回归5项通过：模拟同日修订后，新建研究使用新值，旧保存数据集仍保留原价/hash/版本；源.day字节不变。typecheck通过。
- 真实A500所选贵州茅台、平安银行采集验证成功，excluded为空，末日2026-09-10，source=tdx-local+g4day，分别携带沪深增量版本。报告C:/Users/jm/Documents/tdx-research-increment-audit-20260911.json。使用隔离缓存库，未改生产库。
- 上一批RPS构建及desktop:prepare通过；更早全量238文件通过、5跳过，1333项通过、6跳过。本轮构建与全量回归仍运行。
- 此处证明研究数据采集与版本保留，不表示全部研究指标/交易仿真已完成P4验收。分钟循环、生产调度、三交易日链路及其余P0—P4清单仍待完成。


### P3 收盘增量到RPS入口

- 新增 --close-rps-increment，无需旧全量ZIP成功回执，先运行当前沪深+参考指数增量任务，再读取不可变快照验证当前清单一致、当日参考指数与至少90%有效股票记录；不信任任务自报计数。RPS内部仍重新检验实际行情覆盖。
- 覆盖检查与任务回归4项通过，typecheck和runtime重建通过。上一批研究构建/desktop:prepare及全量240文件、1335项通过（5文件/6项跳过）。
- 2026-09-11约16:50真实隔离入口运行：当日ZIP已发布，增量有效记录5214（含参考指数），已进入当日收盘RPS计算。进程ID32452、exec会话80275尚在运行；仅记录为正在验收，不计完整链路成功。
- 本轮未注册Windows任务、未更新release、未修改生产配置。


### P3 首次真实收盘闭环结果（隔离环境）

- exec80275已正常结束、退出码0；收盘观察rps-observation-2026-09-11-close-46b2e210efa7e3e06e1b已落库。增量门槛计数5207/5224，实际RPS可读取新价5206/5224，沿用12、缺失6；两次覆盖检查未混为同一数字。
- 六周期有效排名数量4924/4925/4926/4922/4926/4919；source含当日增量参考日历和4个实际增量版本引用。
- 本隔离库未启用板块自动批次，发布groups为空。用已发布个股输入另行核验通达信板块：30个一级行业全可算，269个概念中267个可算；这是独立聚合验证，不冒充已发布板块收盘快照。
- 报告C:/Users/jm/Documents/tdx-close-rps-audit-20260911.json。此为首个交易日隔离闭环证据，不等于生产任务或连续3交易日验收。

### P0/P2 Windows控制复核

重新读取Computer Use技能并通过独立node_repl导入@oai/sky。list_apps原生管道返回os error2；重置内核、重新导入后list_windows仍返回相同错误。由此确认不仅是浏览器CUA连接问题，原生桌面管道也不可用。未启动或操作通达信窗口，未声称分钟下载已完成。

### P3 收盘重复调用复用

- 收盘入口在读取证券清单、下载增量之前查询同日同目录已发布收盘观察，存在则返回原ID和reused=true；供应商修订不能由定时重试隐式改写历史观察。
- 2026-09-11 17:04左右，以隔离库quant-increment-refresh-audit-CM378H运行重建后的workflow-runner --close-rps-increment，退出0，复用rps-observation-2026-09-11-close-46b2e210efa7e3e06e1b。
- 相关2文件5项测试、typecheck、runtime:build通过。新增测试断言不调用名录读取和增量下载；未做临时回退的测试敏感性验证。
- 生产任务、release未切换；分钟循环实测及连续三个交易日验收仍未完成。

### P3 Windows收盘调用入口

- 新增scripts/increment-close-task.ps1，默认定位最新release；-CheckOnly只检查bundled Node、工作流文件及增量入口标记，不执行旧runner、不打开数据库。
- 可用-ServerDirectory和-DataDirectory指定已准备的运行包与隔离库。正式执行传递--close-rps-increment，保留0/2/1退出状态；脚本不注册计划任务、不触碰旧下载器。
- Windows PowerShell 5语法/BOM与实际子进程测试2项通过，旧包拒绝、检查不创建数据目录、工作目录/参数/数据环境和pending退出2均已覆盖。兼容标记是启动前检查，不替代完整包实测验收。
- 最终切换时先对待交付包执行-CheckOnly并在隔离库实跑，之后再调整原15:30任务，避免增量和旧全量下载任务重叠；保留20:00财务和CLS安排。当前未切换生产任务。

### P2 完整行情稳定读取

- 核查发现尾部缓存已比较文件身份及ctime，但readSnapshot完整读取仍只比size/mtime。改为从同一只读文件句柄读取，并比较前后及当前路径的dev/ino/size/mtime/ctime/birthtime和实际字节数；不稳定有限重试，所有句柄在finally关闭。
- 新增文件替换/ctime变化且size和mtime不变的负例，以及替换后第二轮稳定的恢复案例；与既有screening回归共21项通过。此项不代表客户端下载或完整分钟循环已验收。
- 上一轮全量回归已结束：241文件通过、5跳过，1338项通过、6跳过；该轮未覆盖本节随后新增的稳定读取改动。

### P3 收盘后续任务衔接

- 核查发现新增量CLI仅发布RPS，漏掉旧收盘入口的信号确认与CLS复盘。两个入口改为共享close-workflow-followups；增量等待时不运行，成功或复用时运行。
- 信号确认抛错仍尝试独立CLS复盘，任何抛错会使入口失败，重试复用RPS后再补后续任务。启用设置与逐项完成去重仍由原服务负责。
- 相关6项测试、typecheck及runtime:build通过。服务可能将个别失败记录为状态而非抛错，CLI成功不等同所有样本已观测成功。
- 继续核查发现intradayHistory本地日线与CLS验证尚未读取应用增量缓存，后续须补消费者与来源版本记录；当前不把入口衔接计为全部收盘业务已完成。

### P3 后续消费者日线增量

- intradayHistory本地日线接入overlay，预选记录追加sourceVersions；在线路径不混合本地输入。收盘确认仍按原契约沿用已固定的历史前缀及完整当日分钟数据，不能用日线增量掩盖分钟缺失。
- CLS验证的股票与上证基准接入增量，source.incrementSnapshots参与内容hash；已有验证记录保持不变。
- 相关5项测试覆盖增量替换、源数组不变、CLS新旧结果共存及版本引用；typecheck通过。待继续核查历史上证文件全量解析问题和日历fallback，分钟实际下载与生产切换仍未验收。

### P3 CLS基准历史异常隔离

- 将研究已使用的基准区间解析提取为tdx-benchmark，CLS共用；保留250条预热并验证区间内记录，不让无关早年异常使近期复盘失去全部基准。增加日期区间合法性校验，研究原导出保留兼容。
- CLS读取前后增加字节长度、ctime和文件身份检查。相关研究/CLS/增量回归4项通过，包含预热内坏数据必须报错的负例；此处不修改策略指标全历史读取口径。

### P3 增量参考日历回退

- 用户日期及在线完整日历仍优先；沪深本地回退时合并已观测、成交量非零的上证增量日期，hash绑定版本，仍明确为非完整交易日历。不用未来采集版本推断当前日历。
- 定向4项与typecheck通过。此前运行中的全量回归在工作区继续变更期间出现新日历用例失败（1失败、1346通过、6跳过），不能计为通过；停止编辑后重跑固定状态，未触发失败后附带的build。

### 固定版本回归与准备目录验收

- 停止代码修改后的exec28606结束、退出0：243文件通过、5跳过，1347项通过、6跳过；build与desktop:prepare通过。此前日历失败未在固定版本重现。
- 使用.next/standalone中的bundled Node，经increment-close-task.ps1指定隔离库CM378H：CheckOnly返回ready，真实调用返回complete/reused=true及原收盘观察ID，退出0。
- 本次验证的是准备目录与已完成观察复用，不等于从空库下载全市场、运行启用后的信号/新闻业务，也不是release/win-unpacked更新或Windows任务安装。
- base.dbf只读审计见C:/Users/jm/Documents/tdx-base-dbf-audit-20260911.json：7943行40字段，无名称与停牌状态字段；缺名按市场+代码仅匹配bj920305，不能借财务表消除缺名。sh688801上市日期为20260911，其他日期0保持未知。

### P3 收盘计划任务切换脚本

- 新增install-increment-close-task.ps1，默认只读预览。实机预览确认原任务15:30仅一个触发器，保留12:00/20:00，其他RPS及CLS任务不改。
- Apply先检查release支持增量、任务未运行且无同名新任务，备份原XML；创建禁用的新任务，移除旧15:30触发器，再启用新任务。配置15分钟失败重试7次、单实例与30分钟运行限时；异常尝试恢复原触发器并撤销本次创建。
- Windows PowerShell语法及现有wrapper子进程回归2项通过；本机仅执行preview，未执行Apply，注册/异常回滚尚未实机验收。旧release不满足增量入口检查，不能计为生产已切换。

### P3 切换失败恢复的隔离验证

- 使用真实PowerShell执行安装脚本，ScheduledTasks及备份写入替换为内存命令，新增3项验证：禁用创建后移除旧收盘触发器再启用；启用失败恢复全部旧触发器并撤销新任务；旧包失败时无备份或任务写入。
- 测试初版出现JS模板插值错误，修正后3项通过；不据此宣称真实Windows任务注册或权限环境已验收。生产任务保持原配置。

### P3 真实下载启动失败诊断

- TDX_DailyDownload于2026-09-11 15:30:30退出1，无当日下载日志。外部tdx_download.ps1无UTF-8 BOM，Windows PowerShell5 ParseFile报try/catch及字符串语法错误；同一原始文件显式UTF-8 ParseInput通过。
- workflow-task改为在独立PowerShell中显式UTF-8加载原脚本，保留MarketOnly参数与原退出码；通达信目录内文件未修改。现有计划任务引用此仓库wrapper，因此下次旧下载入口会使用此编码修复，尚未实际重跑大包下载。
- 相关3项脚本测试通过，包含无BOM脚本的两种参数路径和失败退出7传播。
- 完整包地址https://data.tdx.com.cn/vipdoc/hsjday.zip的HEAD实测返回200/text-html/987字节，非ZIP证据；不能将HTTP200当下载成功。前一交易日20:00日志曾记录成功，目前完整包获取及应用历史缓存仍待实现/验证。

### P3 完整包GET及官方来源复核

- 使用原下载器UA/Referer/Accept加Range读取GET前1024字节，响应200、text/html、988字节，前缀3c7363726970743e（script），无ZIP签名；这次不只依赖HEAD结论。
- 官网https://www.tdx.com.cn/article/vipdata.html当前源码唯一完整包链接仍为//data.tdx.com.cn/vipdoc/hsjday.zip。未发现经官方证实的备用地址，不猜测镜像或执行校验页代码。
- 网络完整包下载目前不能验收；继续实现本地完整包导入应用缓存，待客户端或正常浏览器取得ZIP后再真实验证。此项不能替代分钟客户端下载与完整包自动获取要求。

### P3 完整包选择提取

- 新增inspectFullDayPackage：只读指定ZIP，Windows内置ZIP实现，按明确证券清单提取到受控临时目录；拒绝越界、重复、市场不匹配及残缺记录，限制包/条目/批次体积，读取前后校验源包状态，finally清理本次临时目录。
- 合成固定ZIP四项测试通过；初版PowerShell5 JSON数组嵌套导致漏选，显式string[]转换修复。typecheck通过。
- 仅完成提取和解析接口，尚未发布到应用缓存或接入消费者；真实完整包目录布局尚未验收，不把合成fixture当供应商兼容证据。

### P3 完整包缓存发布

- 新增publishFullDayPackage/readFullDaySnapshot，用现有records保存逐股不可变快照及当前指针，hash绑定证券和原始字节。批次缺股不发布，旧导入时点/日期回退/同时点冲突拒绝；原子事务沿用DB缓存收据清理规则。
- 合成ZIP验证重复导入复用原版本、缺股不写入，以及批次中途失败回滚已写入品种。尚未接入图表/RPS/研究消费者，真实供应商完整包与自动下载仍待验收。

### P3 完整包个股日线读取

- readLocalDailySnapshot在本地日线不可读或完整包日期更新时选缓存；本地日期相同/更新仍优先本地，保留明确来源和版本。RPS、研究、盘中历史及CLS个股日线已调用此入口。
- overlay允许完整包作为基础，保留完整包版本加日增量版本；研究含完整包时显示混合数据来源，不冒充纯本地文件。
- 定向10项及缓存/overlay3项通过，typecheck、runtime:build通过；包含实际合成ZIP导入后本地缺文件仍能读取的证据。
- 图表初始快照、基准日线、证券池发现仍待接入；本地与完整包同日期不同内容的显式修复选择尚未实现。真实ZIP及生产任务未验收。

### P3 图表完整包入口

- snapshot worker日线分支使用完整包回退，5分钟分支保持原读取；图表从分钟切换至日线时也可读取完整包缓存。
- 实际Worker线程读取隔离库导入记录的测试先暴露better-sqlite3被打进bundle导致原生路径错误；worker构建改为external better-sqlite3，与其他DB worker一致。重建后缓存/日增量图表/周期回归11项通过。
- 此项覆盖后台真实worker，不等于桌面视觉验收。基准、证券池发现、同日期显式修复与完整包导入用户入口仍待补齐。

### P3 研究/CLS完整包基准

- 研究与CLS复用readBenchmarkSnapshot：保持区间及250条预热校验，本地不可用或缓存日期更新时使用完整包上证基准，来源版本保留，随后叠加日增量。股票本地但基准来自完整包时研究也标为混合来源。
- 研究/CLS相关4项及完整包缓存2项通过，typecheck通过；新增合成指数ZIP验证无本地指数文件的真实导入读取。RPS自身参考日历、证券池发现、同日期显式修复与导入UI/CLI仍待接入。

### P3 RPS完整包发现及命令入口

- RPS证券清单合并完整包缓存股票，按市场过滤、去重；源目录不可扫描时允许已导入清单继续，缺名仍保留代码。参考日历支持完整包上证最近1000根并保存版本，不替代用户日期。
- workflow-runner新增--inspect-full-day ZIP 逗号证券清单（只检查）与--import-full-day ZIP 逗号证券清单（原子发布）；不依赖已启用每日工作流，便于初始化。未导入全部所选证券时不报成功。
- RPS相关11项、随后新增清单/日历链路的缓存2项通过，runtime重建通过。全局行情浏览目录仍待合并缓存清单，同日期修复选择尚未完成；真实ZIP及生产切换仍未验收。

### P3 浏览股票池发现完整包证券

- 全部沪深浏览池合并完整包缓存证券；指定行业/A500清单仍严格按成员筛选，不把其他导入证券塞入所选池。
- 新增fullDayCache状态，UI区分“完整包缓存日线可用”与真实扫描的本地文件，保留未核实名称，不把缓存伪装为TDX源文件。
- 相关8项测试通过；同日期显式修复、真实完整包、客户端下载和生产三日验收仍未完成。

### P3 同日期显式修复

- 完整包导入支持--repair-same-date，选择记录在当前指针，不改写不可变历史快照；本地日期相同时可明确选缓存，本地推进后仍优先本地。默认及旧指针均不启用修复。
- 个股读取和研究/CLS基准应用该选择；相关6项回归通过。操作及隔离验证说明见tdx-full-day-import.md。
- 当前尚未做真实ZIP价格对账与桌面视觉验收，不将此选项当作自动判定哪一来源正确。

### 完整包批次固定版本验收

- exec4270结束、退出0：247文件通过、5跳过，1361项通过、6跳过；build与desktop:prepare通过。运行期间未继续修改应用代码。
- 本次产物更新到.next/standalone，正式release尚未更新；测试通过不替代真实完整包、分钟客户端及生产三日验收。

### 实机阻塞复核（17:52）

- 新报告C:/Users/jm/Documents/tdx-baseline-20260911-final-check.json：16个5分钟样本末根仍为2022-11-30 15:00，没有下载推进证据。
- 原生sky.list_windows再次返回native pipe unavailable/os error2，未发现TDX运行进程。P0客户端能力未验收，P2循环控制器尚未实现，不能仅标成“已有实现待实测”。
- 正式包更新已请求用户确认但尚无回复，不把自动goal continuation当作打包许可。完整包真实ZIP仍缺、生产任务尚未切换、连续3个真实交易日未完成。
- 可以继续的独立代码已通过固定版本验收；后续需恢复原生桌面控制、取得真实完整包并完成正式交付授权，不能以重复测试替代以上依赖。

### 正式桌面交付与收盘任务切换（2026-09-11）

- 用户明确授权后，pnpm desktop:pack 完成，正式产物为 release/win-unpacked/GuanlanQuant.exe。打包重新执行 build、runtime:build 和 desktop:prepare，均成功。
- 正式包隔离启动 smoke 通过（exitCode 0），源行情文件未变化；bundled Node 的合成完整包导入及隔离收盘观察复用入口均成功。未将合成 ZIP 验证当作真实完整包验收。
- Quant_RPS_CloseIncrement 已注册并启用，15:30 执行；按用户最新要求，失败后最多重试 1 次、间隔 15 分钟（取代之前的 7 次配置）。安装脚本与实机配置同步。
- TDX_DailyDownload 已移除 15:30，保留 12:00、20:00；尾盘及 CLS 任务未修改。原任务备份位于 C:/Users/jm/Documents/QuantWorkflowBackups/increment-20260911-175943。
- 以上验证使用隔离数据目录，未手动执行生产收盘计算；真实生产连续三个交易日、真实完整包对账及分钟客户端下载控制仍未完成。
