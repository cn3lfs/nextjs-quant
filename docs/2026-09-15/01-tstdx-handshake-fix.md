# tstdx 握手修复（2026-09-15）

## 问题与最小修复

旧客户端可以建立 TCP 连接并收到三次初始化应答，但 K 线正文只有 `20 03`，五档返回零条。官方客户端在同一节点能够取得完整行情。

本次只把第三条 `0x0FDB` 初始化报文的末字节（整帧偏移 41）从 `0x02` 改成 `0x05`。其余握手、38 字节 K 线请求、五档请求和解码器保持原协议实现。末字节正式语义尚未确认，不命名为权限、版本或认证标记。

不复制官方客户端 292 字节首帧，也不引入抓包中的用户或设备身份数据。原始抓包仅保留在本机临时目录，不纳入仓库。

## 单变量对照

在 `124.71.187.72:7709` 的独立新连接中测试宁德时代：

| 初始化变体 | 日 K / 旧五档 |
|---|---|
| 原三条握手 | 两字节 K 线正文 / 零报价 |
| 仅替换官方第一条 | 仍失败 |
| 仅替换官方第二条 | 仍失败 |
| 仅替换官方第三条 | 恢复 |
| 第三条仅替换名称区域 | 仍失败 |
| 第三条仅替换偏移 23–29 区域 | 仍失败 |
| 第三条仅末字节 02 → 05 | 恢复 |

官方 54 字节 K 线请求在旧握手下也失败；原 38 字节请求在修复握手后成功。因此不需要扩大 K 线请求长度。

恢复样本：日 K 三根，末条 `2026-09-14` 收盘 337.11；5 分钟 K 三根，末条 `2026-09-14T15:00:00+08:00` 收盘 337.11；五档完整买卖各五档。

## 真实节点和应用验证

只读读取本地 `connect.cfg` 的 43 个普通行情主站，对同一标的分别查询日 K、五档、20260914 历史分时，每台三次、节点并发 2。修复前后各 129 次业务请求：

| 能力 | 修复前成功 | 修复后成功 |
|---|---:|---:|
| 日 K | 0/43 | 43/43 |
| 五档 | 0/43 | 43/43 |
| 历史分时 | 43/43 | 43/43 |

应用 `tstdx-adapter-live.test.ts` 实网通过：沪深股票、创业板、科创板、北交所、沪深指数、沪深 ETF、通达信板块指数共十个日 K 样本均为 `ok`，腾讯专属 `pt` 标识仍不冒充通达信代码；历史分时与四类搜索同时通过。

本轮结果反映测试时点，不承诺所有节点永久可用。节点测速仍需按查询能力判断；本次未改节点优先级、冷却或自动回退策略。

## 回归与边界

`packages/tstdx/tests/transport.test.ts` 使用固定真实行情正文与分片/压缩假服务端，验证 05 握手可解析行情，并以 02 握手为负对照，拒绝不完整 K 线和零报价；测试不依赖公网。fixture 仅包含公开行情数据。

此修复只处理初始化导致的 K 线/五档不可用。此前发现的 ETF 分时/报价精度、指数成交量含义、快照时刻解码仍是独立正确性问题；不能把“返回了数据”扩大为这些字段全部通过验收。

包离线测试 93 通过、1 个实网用例默认跳过；应用实网 adapter 用例 1 通过；全量测试 2241 通过、23 跳过（308 个测试文件通过、13 跳过）；typecheck 与应用构建通过。应用测试及构建均使用独立的临时 QUANT_DATA_DIR，未使用生产数据库；未打包桌面程序。
