# 数据源适配执行记录

对应[执行计划](data-source-adaptation-plan.md)，2026-09-15 开始。未提交、未打包；所有应用验证使用临时 QUANT_DATA_DIR。

## S1：tstdx 字段正确性

独立实网对照沪深三只 ETF、两只股票、两个指数：读取证券目录、五档、日 K、2026-09-14 历史分时和历史成交。

- ETF 目录 decimalPoint=3，股票/指数为 2。ETF 原分时、成交、五档统一除以 100，导致 50ETF 返回 29.76，而日 K 为 2.976；改为按该源目录精度解析。目录缺失或非法时拒绝猜测。目录缓存限定客户端生命周期。
- 包原始解码函数增加可选精度参数，默认两位保持调用兼容；客户端对非股票/指数自动查询目录。直接使用 wire 解码器的调用方必须提供证券精度。
- 指数没有可交易五档，原报价区域可包含涨跌家数等统计字段（上证买一数量 1330、卖一数量 928，与指数 K 线涨跌家数一致）；客户端返回空盘口，避免展示虚构价格。
- 上证历史分时字段累计 77928096，日 K 成交额 779281235968；创业板分别 40067545 与 400675700736。比例接近一万，支持“金额尺度”解释，但不是正式协议定义；adapter 明确标记指数源字段不能用作股/手成交量，暂不转换。
- 股票/ETF 日 K 量与分时累计量比例约 100，存在自定义浮点舍入差异；指数日 K 量与报价量接近同尺度。保留各接口源单位，不统一乘除 100。
- 快照时间按 [pytdx issue 187 的两种编码线索](https://github.com/rainx/pytdx/issues/187#issuecomment-441270487)修正候选解析；原始整数保留。ETF 实网出现超过 24 小时的编码，正式含义未确认，返回 null，消费端拒绝以未知时间判定新鲜行情。此项不能宣称全部品种时间解码完成；还需要官方客户端或独立带时间来源对照，不能除以十猜测。

固定公开行情报文放 packages/tstdx/tests/fixtures/precision-market-response.json；股票/ETF/指数分时和历史成交末价与独立 K 线收盘一致，旧两位精度作为 ETF 十倍错误负对照。包测试 99 通过、1 个实网默认跳过；typecheck 与 adapter 定向 23 项通过；全量 2241 通过、23 跳过；应用构建通过。应用实网 10 品种 × 日/周/5 分钟共 30 组通过，搜索和历史分时同时通过。

剩余外部证据：快照时间的当前客户端对照、指数源字段正式单位。未知字段有明确保护，不妨碍后续其他源适配。

## S2：东方财富 HTTP adapter

新增 eastmoney-adapter.ts，复用拆出的 eastmoney-bars.ts 校验器；chart-history.ts 和 online-chart-data.ts 均调用同一 adapter，消除重复请求实现。

- 原生标识：沪市 1、深市/北交所 0、板块 90；板块使用 emBKxxxx，验证响应代码及市场，不映射腾讯 pt 名称。S2 阶段仅 adapter 接入，S4/S6 后续完成通用图表页面。
- 独立 HTTP 查询确认十个品种日线有数据；adapter 日/周/5 分钟共 30 组实网通过，覆盖沪深北、科创板/创业板、沪深指数、沪深 ETF 和原生板块。
- 上游忽略 lmt 时在校验后按数量截取。范围首尾按日包含；请求参数、原始返回根数、来源版本保留；数量不足不推断全部历史。
- 每项返回状态，错误不跨源、不重试；支持取消和单请求 15 秒超时，流式读取限制 8 MiB。空响应、串码、错误和非法日期不得作为成功。
- 来源默认不复权，qfq/hfq 参数单独记录；此次真实矩阵为不复权，不代表全部除权样本通过。分钟日期与股/手语义仍按来源保留，跨源一致性留 S6。

定向检查与实网矩阵已通过；新增范围、复权参数及超大响应测试通过，typecheck、全量 2247 通过/24 跳过、应用构建通过。剩余特殊证券与真实页面验收不冒充完成。

## S3：本地 vipdoc

通过薄 vipdoc-adapter 显式启用图表基金，复用 readSnapshot 和现有聚合/缓存契约，不新增一套读取器。沪深场内基金日线整数按三位精度读取，分钟浮点价格不再额外缩放；原 readSnapshot 默认门禁、A 股扫描和研究尾窗限制保持。完整包导入使用相同精度，基金解码版本进入缓存指纹；旧两位精度基金缓存不可复用，未改写或删除生产数据。

真实只读矩阵 30 项：26 项完整解析成功，2 项缺文件（北交所/通达信板块分钟），2 项错误（上证指数日/周共同遇到 1991-05-09 非法历史记录）。不能把矩阵脚本成功当成 30 项数据全部可用。成功样本日线末日为 2026-09-11、分钟末时刻为 2022-11-30 15:00；这些数据未冒充最新行情。

固定真实尾段覆盖股票/指数/沪深 ETF，校验 4.579、3.341 等独立已知收盘；损坏/空文件、重复日期、未知精度及研究证券范围有回归。完整包 fixture 初版路径不符合 market/lday 格式，修正 fixture 后导入与旧基金缓存隔离测试通过。初次全量发现 5 项复盘测试要求原读取门禁不变，因此改为仅图表 adapter 显式启用基金，而非放宽默认读取器；修正后复盘/图表相关 25 项通过。该阶段 typecheck、构建和全量 2250 通过/25 跳过。

## S4：westock-data 与板块页面

再次直接运行本地 Node skill，再测 adapter，未用程序调用成功替代原始工具验证。默认日线与 ETF 分钟 raw 命令分别对照；应用矩阵 21 项支持通过、3 项明确不支持。例：上证腾讯量 4588889、东方财富 458888916，相差约 100；股票金额存在取整差异。adapter 版本 2 增加单位与舍入提示，不把未知源单位拼到本地量。

图表专用 symbolSchema 接受腾讯 pt 与东方财富 emBK；证券搜索支持输入完整源代码，快照、周期读取、缓存、页面贯通。板块禁用持仓/RPS、身份与自选操作，未放宽交易及研究证券池。日/周板块真实 API 和页面通过，腾讯分钟明确 unsupported，无隐式回退；东方财富原生板块分钟成功。修复禁用查询仍显示“读取中”的提示。

增加新股短历史、零成交量、空结果与代码门禁回归。这些固定测试不等于真实停牌、除权、退市/代码变更样本全部完成；对应实网特殊样本仍列待补。该阶段全量 2253 通过/26 跳过，typecheck、构建通过。

## S5：东方财富 MCP 11 类工具

按本地 skill 的实际 11 类工具分别请求 A 股、基金、债券、指数、港股、美股、综合、宏观、筛选、新闻、公告。固定请求见 tests/fixtures/mx-tool-requests.json；无凭证或请求头入库。

11 类均有非空数据；9 类本次请求正常，新闻/公告两个请求语义部分不符：新闻 20 行混入 3 行公告；公告 20 行中 16 行超出请求日期、15 行为新闻。版本 2 增加 scopeWarnings，返回 with-message 并在页面显示警告；保留原表，不按预期筛掉行来宣称成功。只对明确日期范围与已知字段检查，不能替代所有自然语言语义验证。

应用 API 11 类复测通过（验证路由、非空返回与问题提示，非 11 类语义全部通过）；相关离线测试 34 项通过。MCP 保持独立资料查询，不接图表、不影响四源优先级。授权失效等错误采用已有固定传输回归，不人为破坏本机凭证来做实网负例。

## S6：应用验收与优先级

使用隔离 QUANT_DATA_DIR 启动实际构建，Playwright 操作页面；未使用默认生产库，也未修改通达信目录。当前已完成：

- 四源手动选择及同一 ETF 的 5 分钟切换。local 末时刻 2022-11-30 15:00 且显示历史不足/过期；westock 192 根、东方财富 1488 根，末时刻均为 2026-09-14 15:00；tstdx 2000 根，盘前已返回 2026-09-15 09:35 零量形成中柱，页面明确标记形成中，不能作为已成交证据。
- local ETF 日线读到 2026-09-11，源价格 4.579；页面初测发现通用显示仅保留两位，补修基金 OHLC、价格轴和画线三位精度，股票/指数仍两位。
- 腾讯 pt01801081 日线 1210 根、周线 256 根；5 分钟明确 KLINE_001 不支持。东方财富 emBK0475 5 分钟 1488 根；切换错误不会伪装成另一个源成功。
- 设置页保存 westock-data，刷新整页后默认选择与实际图表均为腾讯。页面来源在周期/品种切换后保留，首次请求读取持久设置，再同步选择框。
- 最终构建页面复测：本地 ETF 的 OHLC 和价格轴均显示 4.579；腾讯板块不再显示禁用的持仓查询读取提示；MCP 公告原表上方实际显示 16 行日期越界与 15 行类别不符的警告。

同日期 2026-09-14、日线、不复权收盘对照：

| 品种 | tstdx | 东方财富 HTTP | westock-data |
| --- | --- | --- | --- |
| sh600000 | 9.4 | 9.4 | 9.4 |
| sz300750 | 337.11 | 337.11 | 337.11 |
| sh000001 | 3885.33 | 3885.33 | 3885.33 |
| sh510300 | 4.552 | 4.552 | 4.552 |
| sz159915 | 3.304 | 3.304 | 3.304 |

腾讯原始默认日线命令的 ETF 为 4.55/3.3；应用显式 bfq 结果为 4.552/3.304，不能拿默认复权结果认定程序丢失精度。实跑 adapter 参数为 symbols 上述五只、period=day、limit=3、adjustment=none、end=2026-09-14；记录的 CLI 参数是 `["kline","sh600000,sz300750,sh000001,sh510300,sz159915","--period","day","--limit","3","--fq","bfq","--end","2026-09-14"]`，执行器另加 raw 输出与既有兼容 preload。

量额不作相等断言：例如 sh600000 的 tstdx 量 77187104、金额 722307392，东方财富量 771871、金额 722307374，腾讯量 771871、金额 722310000。指数成交量尺度与普通股票不同；这也是保留来源单位、拒绝跨源直接拼接的依据。本地末日为 9 月 11 日，不与 9 月 14 日进行伪同日比较。

后续按用户指定调整自动顺序：本地 → tstdx → 东方财富 HTTP → westock-data。本轮来源覆盖和单位对照不足以建立长期在线可靠性排名，未用单次节点成功率宣称长期稳定，未新增自动轮询。不同源的历史深度、形成中柱、分钟可用范围和量额尺度均不能自动拼接。

## 未闭合的外部证据与验收边界

1. tstdx ETF 特殊快照时间编码与指数分时量额的正式定义未知，已返回 null/源字段标记；需独立协议或官方客户端时点对照。
2. 本地北交所/板块分钟缺文件、上证早年非法历史记录、旧分钟数据时效属于源文件问题；本轮没有下载、修写或跳过坏数据。
3. 真实停牌、除权、退市/代码变更全套样本，以及跨交易时段/多日可用率尚未齐备；固定边界测试不替代这些实网证据。优先级按用户指定执行，不能据此宣称长期可靠性。
4. MCP 新闻/公告存在供应商返回范围不符，已可见提示，不能称为全语义通过。

临时原始探测、实网 JSON 与浏览器快照在系统临时目录 quant-pytdx-issues-review；可停掉本轮服务后清理该目录及 quant-source-plan-* 隔离验证目录，不要删除生产数据目录。持久回归与公开小型 fixture 留在 tests/ 和 packages/tstdx/tests/。

## 最终检查与复现入口

- 独立 tstdx 包：99 通过、1 项实网默认跳过；全项目最终 312 个文件通过、16 个文件跳过，2255 项通过、27 项跳过。
- typecheck、pnpm build（含 runtime/worker 重建）、format:check、git diff --check 通过。未打包、未提交、未推送；src/server/mcp.ts 未修改。
- 应用测试全部使用隔离 QUANT_DATA_DIR；现有失败分支测试会输出“研究使用台账写入失败”，测试最终通过，该输出不代表写入生产台账。
- 实网测试按需启用：QUANT_TSTDX_ADAPTER=1 对应 tests/tstdx-adapter-live.test.ts；QUANT_EASTMONEY_ACCEPTANCE=1 对应 tests/eastmoney-adapter-live.test.ts；QUANT_VIPDOC_ACCEPTANCE_ROOT 指向只读通达信根目录，对应 tests/vipdoc-source-live.test.ts；QUANT_WESTOCK_ACCEPTANCE=1 对应 tests/westock-acceptance-live.test.ts；QUANT_MX_TOOLS_ACCEPTANCE=1 对应 tests/mx-tools-live.test.ts。都先设置独立 QUANT_DATA_DIR，再用 pnpm exec vitest run 指定文件。默认跳过避免普通回归依赖实时网络、行情变化和本机配置。
