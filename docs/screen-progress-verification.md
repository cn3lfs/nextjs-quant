# 规则与自动快评阶段进度

2026-09-09，补充原方案模块 C 的独立状态和阶段计数。

规则 worker 的读取和复核阶段上报已处理/总数、本阶段失败与隔离数；读取失败不重复计入复核阶段。保存按50个候选一个批次计数，批次间让出事件循环并检查取消，取消后停止后续批次。最终汇总以全池证券数量、读取异常和隔离结果为依据。

自动快评的准备方法、共享资料、每批获取证据、模型执行、保存与缓存复用分别记录阶段、计数单位和分母。模型批次失败记录1个批次失败，不把未执行的后续批次算成已处理。旧失败结果的暂停范围仍保留；阶段失败计数不等于暂停范围内证券总数。

`screenTaskProgress` 按规则任务 ID 查询该任务和关联自动快评，两个卡片分别显示状态、阶段、进度和总历时（含排队，保留0.01秒）。活动任务每2秒更新此轻量状态；结果和输入不在接口内，规则结果仍可独立分页读取。全局80条摘要不增加阶段对象，维持已有响应预算。任务历史状态详情也可查看计数；旧任务未记录的计数明确显示缺失。

证据与范围：

- 专项测试覆盖读取失败/旧行情隔离不混计、共享准备失败、首批模型失败后不调用第二批、规则已完成时模型仍可运行，以及关联状态不返回输入/结果。模型调用均为测试替身，没有运行真实模型。
- 全量140文件664测试通过；类型检查、生产构建（含runtime）、桌面prepare/smoke通过。随后仅把两个耗时文本从整数秒改成小数秒，并再次通过类型检查、构建、桌面冒烟。
- `output/screen-progress-benchmark.jsonl`：当前扫描池日线5895只、五分钟5193只，各首次＋5次重复。首次日线7328.13ms，重复中位392.31/P95 409.10ms；五分钟首次5235.09ms，重复中位247.26/P95 259.72ms。各周期重复结果hash一致，最终计数与结果吻合。计时含服务端建任务至持久化完成，不含扫描、UI/HTTP和OS冷缓存重置；这是本次单进程回归，不替代原多进程基准。五分钟源截至2022-11-30，不证明实时分钟源就绪。
- `output/screen-progress-http-verification.json`：真实规则任务5193/5193，0读取失败、463隔离，与已存结果一致；关联界面样本的AI任务为失败，接口独立显示，报告数为0。
- `output/playwright/stage-progress-running.txt`、`stage-progress-failed.txt`、`stage-progress-final.txt`：浏览器显示规则完成/AI运行中，后续自动更新为AI失败，规则结果始终可见；最终亚秒耗时显示0.22秒。AI状态由明确的隔离界面样本构造，不冒称真实模型执行记录。

排队、文件版本检查等已有阶段名称保留，尚无数值计数时不补零；此增量不证明所有深研、在线筛选、新闻等异步流程均已统一阶段计数。所有A–H原要求的最终验收仍未完成。本轮未修改用户生产设置、发送通知、调用DeepSeek或打包EXE。
