# 2026-09-11 工作流实施与验收

本次用户已批准实施。以下接续 E0–E4，代码完成与真实业务验收分别记录。

| 阶段 | 交付 | 状态 |
| --- | --- | --- |
| F1 | 行情图表置顶；七周期行情与结构共用快照；形成中标识；分页与历史拖动 | 已实现，真实源及浏览器验证通过 |
| F2 | 12:00 下载完成后午盘 RPS、14:40 批量行情估算、15:30 下载后收盘 RPS；20:00 修复；后台运行 | 已实现并安装；午盘计算实测，自然触发待到时 |
| F3 | A500 默认全量、可选子集、名单冻结；真实样本与重放 | 已实现，真实子集重放及选择交互通过 |
| F4 | 财联社 08/12/20 采集完成驱动分析，盘前主样本固定，两次增量；完成标记 | 已实现；正式午间后台链路已完成 |
| F5 | 完整检查、桌面打包与实际交易日联动验收 | 软件交付检查完成；未来时段及成熟收益待验证 |

## 已确认口径

- 日/周/月/5/15/30/60 分钟均可绘制缠论及双突破。保留策略研究的完成日线契约，图表结构单独使用目标周期快照。
- 分钟聚合不得跨午休；历史缺条不补造。当前未完成周期及结构显示“形成中”。在线优先复用项目 MCP，再用明确来源的后备。
- RPS 为沪深统一分母。失败品种沿用旧价并标记；新价覆盖率不足 90% 不发布新批次。午盘、尾盘快照不覆盖收盘历史。
- 原 11:20 预选只能读取此前成功批次，不读取当日未来的午盘批次。
- 仅已授权下载器可更新通达信文件；任务与脚本先备份。桌面和后台须共享持久任务认领，避免重复发布。
- A500 使用当前名单须提示生存者偏差。公司行动完整结算、其他策略技能、RPS 对照实验本轮不扩展。
- 盘前主样本不得被午间/晚间新闻覆盖；按新闻 ID 去重，成功后推进检查点。没有完成标记不自动消费报告。
- 新迁移必须先更新打包程序再由生产运行时应用。开发测试始终隔离数据目录。
- 待自然到期的 T+1/5/10、未来时段验收保持待验证，不以单元测试替代。

## 本次交付证据（2026-09-11 13:06）

- 全套 Vitest：228 文件通过、5 文件跳过；1303 用例通过、6 跳过。另 opt-in 真实 MCP 检查覆盖 sh600000、sh000001 七周期并调用真实缠论 DLL 与双突破。typecheck、build/runtime、desktop:prepare、desktop:pack、打包后 smoke 均通过。smoke 的本地源文件保持不变。
- 图表：真实 MCP 按 700 根分页取得最多 2000 根，历史耗尽时保留实际根数。浏览器逐个切换七周期，行情、结构和来源一致；MCP 工具返回错误时实测整体回退东方财富。后备源的分钟历史少于 2000 根时明确显示实际数量，不声称完整。
- 交互：图表位于股票池之前；鼠标拖动改变所看日期，拖至边界从 360/505 补到 505/505，切回周期后保留已加载视图。支撑压力值位于图上方，分钟图例与信号摘要为中文日期和 24 小时时间。
- RPS：隔离真实午盘计算用时 60794ms，当前沪深名录 5223 只，取得当批新价 5219 只（99.9234%），缺少 4 只，包含行业及概念聚合。实际报价来源为腾讯批量接口；直接通达信节点无有效报价，MCP 逐只补取覆盖不足时不会发布。该验证没有把隔离数据库复制到正式库。
- A500：实读 500 只成分；浏览器验证全选 500、取消一只为 499、清空后不能开始。真实选定 sz000001/000002/000032/000034/000035，2026-07-01 至 2026-09-10 双突破研究得到 1 个事件，冻结数据重放 hash 完全相等。样本不足以评价盈利性，没有历史交易条件时不生成净值及夏普。
- 财联社：正式 Windows `CLS_News_Collector` 于 12:57:57 启动，量化应用关闭时完成采集与分析，LastTaskResult=0；269 个新闻 ID，完成回执时间 12:58:36。报告位于 `C:\Users\jm\Documents\QuantWorkbench\CLS\CLS-2026-09-11-noon.md`，同路径 `.ready.json` 校验 hash，后台重跑返回同一报告、不重复分析。午间批次不建立或覆盖盘前主样本。
- 桌面包随后按用户要求追加宽屏布局和默认成交量/MACD 双副图：`release/win-unpacked/GuanlanQuant.exe`，13:14:10 生成，已重新启动。1920 宽屏图表为 1622px、1440 窗口为 1142px，均无横向溢出；三个图区共用时间轴。没有新增数据库迁移，没有提交或推送，没有修改用户维护的 `src/server/mcp.ts`。

## 已安装日程与恢复

| Windows 任务 | 触发 | 完成后行为 |
| --- | --- | --- |
| TDX_DailyDownload | 工作日 12:00、15:30、20:00 | 前两批仅行情；20:00 保留财务下载。成功回执后运行对应 RPS；20:00 只修复缺失收盘批次 |
| Quant_RPS_Late | 工作日 14:40 | 通达信优先、腾讯批量后备，保存尾盘 RPS |
| CLS_News_Collector | 原 08:00、12:00、20:00 | 08 点仅采集；午间和晚间采集成功即分析新增新闻 |
| ClaudeCLS-DailyNewsAnalysis | 原 08:30 | 等待当天盘前采集回执，生成主报告并按既有规则固定样本 |

2026-09-14 修订：12:00 不再下载。全量日线包收盘后才发布，午间下载只是把上一交易日的文件重写一遍（实测见 [决策](../decisions.md)）；12:00 改由新任务 `Quant_RPS_Noon` 只做午盘观察（`-Phase noon`，不接触下载器、不写回执），`TDX_DailyDownload` 只保留 16:00。上表的 12:00/15:30/20:00 与 20:00 财务下载是当日历史记录。

全部任务采用当前用户 Interactive 登录方式，可在量化应用关闭时运行，需要 Windows 用户保持登录。旧 08:30 任务的 Password 凭证更新失败，已改为与采集任务相同的主体。原 XML 和下载/采集脚本备份：`C:\Users\jm\Documents\QuantWorkflowBackups\20260911-125554`；XML 已按声明的 UTF-16 保存并验证可解析。新脚本保持 UTF-8 BOM，实际 Windows PowerShell 5 解析通过。

恢复时先停用新 `Quant_RPS_Late`，用备份 XML 恢复原三个任务、恢复两个原脚本，并关闭 records 中的 workflow-config；旧 Password 任务若恢复运行仍需有效登录凭证。无需回退数据库结构，也不删除研究快照。

## 明确待验证

- 安装后的首次 14:40、15:30、20:00，以及下一交易日 08:00/08:30/12:00 自然触发；下载 ZIP 的时效与下载完成后收盘 RPS 全链路尚未到时实测。
- 正式库本日午盘 RPS 未补造，下一次计划批次为 14:40。若新价覆盖不足 90% 或收盘文件仍旧，保留旧排名并记录失败。
- 盘前主样本的 T+1/5/10 实际结果随交易日成熟后验收，不将午间报告回算成盘前预测。
- 2026-09-14 修订后的自然触发待验收：下一交易日 12:00 `Quant_RPS_Noon` 应在不调用下载器、不写回执的情况下产出午盘观察，且通达信下载日志当日无条目；16:00 `TDX_DailyDownload` 仍下载并在回执后计算收盘 RPS。人工冒烟（隔离数据目录跑 `--configure` 后 `--phase noon` 成功、生产包装 `-Phase noon` 幂等返回当日既有观察）不等于自然触发。
