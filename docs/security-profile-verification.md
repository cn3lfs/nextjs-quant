# 证券主档来源展示

2026-09-09，模块 A 增量。

行情页增加按需展开的“证券主档与名称来源”。所选证券通过独立接口读取主档中的名称、代码、市场、品种、币种、名称来源、历史别名和记录时间，并显示当前资料目录。该接口只返回所选证券，不把整份证券目录传入面板。当前主档交易状态始终未核验，上市/退市日期尚无字段或证据，界面明确未取得，不用 TNF 名称推断交易状态。

身份核验完成以及保存数据目录设置后，主档面板缓存失效，名称/下拉数据同步刷新。该面板独立于远端双源身份核验结果，没有触发远端查询。

验证：

- 专项覆盖别名及 TNF 来源保留、仅所选主档投影、未知代码 null、切换资料目录后读取新目录。5 项身份测试通过。
- 全量 143 文件 / 674 测试、typecheck、build（含 runtime）、desktop:prepare、desktop:smoke，以及涉及文件 Prettier 检查通过。
- 生产 HTTP 在隔离数据库读取 sh600519，名称贵州茅台、来源 tdx-tnf、交易状态 unknown；sh699999 返回 null。结果 `output/security-profile-http-verification.json`。
- 浏览器展开后显示“通达信本地 TNF 名称资料”、暂无别名、名称记录时间、未核验交易状态、未取得上市/退市日期和 E:/new_tdx64 目录。证据 `output/playwright/security-profile-collapsed.txt`、`security-profile-expanded.txt`。
- 最后复验隔离证券名称样本：原候选任务、快照、导出不变，报告数仍为 0。

未调用模型、发送通知、写通达信源或打包 EXE。本增量是主档资料可见性，不代表历史上市/退市资料已补齐，也不代表完整 A–H 目标完成。
