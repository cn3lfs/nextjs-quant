# W8 逆回购方向交付（2026-09-13）

仅修改逆回购：费用方向和逐品种数量配平共用实际资金符号，原始 kind 保留；新增方向不可判计数及逐笔冲突 warnings。普通证券判定不变。

## 真实数据验收

只读 `.test-data/statements-v2/lishi20-26.xls`，经 `parseDeliveryTable → importDeliveryTable → reviewTrades` 实际解析与复盘，不写数据库。源文件 SHA-256：`50f36c587e751729f1f6bd57ca806c67611095efa17f19918142e36f61b22198`。

| 指标                             |             修改前 |                            修改后 |
| -------------------------------- | -----------------: | --------------------------------: |
| reverseRepo.interestIncome（元） |               null |      904.9800000001424（+904.98） |
| capitalCommitted（元）           |  8626091.299999997 | 8626091.299999997（8,626,091.30） |
| 已平仓回合总数                   |                785 |                               785 |
| 有效回合 / 排除回合              |            784 / 1 |                           784 / 1 |
| 胜率                             | 50.89285714285714% |                50.89285714285714% |
| 盈亏比                           | 0.8912012172774283 |                0.8912012172774283 |

非逆回购完整输出（去除 reverseRepo 后，含 movingAverage、fifo、exceptions、tradePoints）的修改前后 SHA-256 完全一致：`2d90dd70e363944ddf226608af4134da367491ec59dd601c9404f44c66eefbae`。

232 笔逆回购，方向不可判 0 笔。30 笔冲突全部留痕：204001 为 29 笔、839,042.95 元；131810 为 1 笔、27,002.62 元，合计 866,045.57 元（任务书 866,046 为四舍五入）。警告包含成交索引、代码和“操作列与资金方向不一致，以资金方向为准”。

## 任务书差异与局限

- 785 为已平仓总回合数，胜率有效分母是 784；另 1 个被既有逻辑排除，保持不变。
- 零发生额未定义，按不能证明方向处理，与 null、非有限数一样计数并令利息留空。
- 当前只有汇总 interestIncome，没有逐品种收益接口；任一品种未知或未平使汇总留空，未扩接口。
- 未新增依赖、迁移，未提交、推送或打包；未操作生产库。测试不手设 QUANT_DATA_DIR，沿用已有自动隔离。构建使用独立临时数据路径。
- 本批验收覆盖真实文件导入解析到复盘核；未执行浏览器点击或生产账户持久化导入。实际购回包含利息时，既有费用推断可能退回费用分项，本批不扩大为本金/利息/费用拆解。

## 复验

PowerShell：`$env:W8_REAL_DATA='1'; pnpm exec vitest run tests/w8-repo-direction.test.ts`。真实数据用例需本地原账单，默认不要求其他环境持有私人数据；本次已显式启用并实际通过。持久测试另覆盖手算配平、未配平、跨品种、null/NaN/Infinity/零、双方向标签冲突、费用推断和既有 fixture 完整输出护栏。

## 检查结果

- `W8_REAL_DATA=1 pnpm test`：292 文件通过、5 文件跳过；1984 测试通过、6 跳过，耗时 219.27 秒。既有跳过项保留，未删除或跳过既有测试。
- 最终 W8 定向测试（真实数据已启用）：9/9 通过。
- `pnpm typecheck`、`pnpm build`（含 runtime worker 重建）、六个改动文件 Prettier 检查、`git diff --check` 均通过。
- 全量测试输出过“研究使用台账写入失败，本次运行可能未记录”，最终全部执行项通过；本批未改动该链路。
- 临时基线脚本与包含原始复盘结果的 JSON 在完成验证后清理；仅保留本交付说明及持久测试，不复制私人账单进入 fixture。
