# W5b 分类修正交付

本批仅修正对账单 `cashFlowsOnly` 分类及核对输出，不新增依赖、迁移、提交、推送或打包；未修改 `src/server/mcp.ts`。真实样本只读，未复制进 fixture，实际入库验证使用显式 `:memory:` 数据库并关闭，未接触生产库。

## 改动

- `src/lib/delivery-import.ts`：收窄结息/费用识别；同批结息对按余额恒等式选入账腿；明确逆回购排除；支持成功银行返回码方括号；返回去重前各类已识别现金流条数/金额。
- `src/server/portfolio/delivery-import-service.ts`、`src/server/portfolio/delivery-store.ts`：预览、导入返回及批次 payload 携带 `cashFlowSummary`，不将其误称为新增入库合计。
- `tests/w5-statement-import.test.ts`、对应 `tests/__snapshots__/w5-statement-import.test.ts.snap`：真实结构、未知利息/扣款、混合排序、无金额阈值、银行返回码和默认 all 修复前完整快照护栏。
- `docs/decisions.md`、`docs/next-plan.md` 及本文：事实、冲突、交付范围。

## 真实样本核验

只读 `.test-data/statements-v2/duizhang20-26.xls`，解析到 2969 条数据行。调用真实解析与导入服务向内存库导入，实际保存 123 条现金流、0 条成交；期初证据为 0。

| 类别         | 条数 |  合计（元） |
| ------------ | ---: | ----------: |
| transferIn   |   64 | +1017222.79 |
| transferOut  |   30 |  -788997.34 |
| dividend     |   10 |    +1516.00 |
| interest     |    4 |       +0.82 |
| fee          |   13 |     -264.90 |
| subscription |    2 |    -1000.00 |
| neutral      |    0 |        0.00 |

共丢弃成交 2690 条，其中明确“融券回购购回日”30 条均不进入现金流；排除结息非入账腿 4 条。原 fee 的 36 条中，13 条是股息扣税（1 条 -9.60，12 条 -255.30），23 条是申购扣款 -387800，后者保留 unresolved。

未解析共 152 条（其中数量符号拒绝 25 条），每行保留原因：

| 结构                        | 条数 | 原始发生金额合计（元） |
| --------------------------- | ---: | ---------------------: |
| 到期日[…]，利息[…]，金额[…] |   86 |            -7760082.64 |
| 利息结转[…]                 |   18 |                +181.03 |
| 申购扣款                    |   23 |             -387800.00 |
| 其他数量符号拒绝            |   25 |             +387914.13 |

以上未解析金额是源证据，不是任何现金流类别，也未计入收益。

## 验证

- 修复前反例出现预期失败：未知利息被计入，以及混合排序漏掉成对入账；修复后定向 89 项通过，格式化快照后 W5/W5b 20 项通过。
- `pnpm typecheck` 通过；`pnpm runtime:build` 通过。
- `pnpm test`：289 个文件通过、5 个文件跳过；1962 项通过、6 项既有跳过，耗时 209.78 秒，本批未增加跳过。改动文件 Prettier 与 `git diff --check` 通过；Vitest 会重写快照外层格式，最终按 babel parser 格式化快照，未改快照字符串内容。
- 三份既有交割/历史/银行 fixture 的默认完整结果 SHA-256 修复前后完全一致；新对账单 adversarial fixture 的默认/显式 all 保持修复前完整快照。

## 冲突与未处理风险

- 本批真实样本核验要求优先于 W5 旧任务的交付后交管理者核验要求；未运行真实账户组合复盘或调整 openingCash。
- 默认 all 零变化约束使修正限定在 cashFlowsOnly；all 仍有旧分类风险。
- W5b 对明确逆回购“一律丢弃并计数”优先于 W5 数量符号拒绝；其他成交继续符号校验。
- 真样本发现 7 条成功银行返回码使用方括号，旧正则未识别；本批补上。结息对在日期倒序文件中仍正序，不能统一反转同时间行。
- 运行测试不手动设置 QUANT_DATA_DIR，使用仓库既有 setup 隔离机制。
- 未确认的其他利息及申购结构保持 unresolved；本批结果不能宣称完整资金重建，TWR、现金残差及最低现金未重跑。
- 旧错误批次不会自动改写；同文件重复导入仍幂等，管理者需核对撤销旧批次后再导入。汇总是去重前源数据，不是重复导入新增金额。
- 本批未做浏览器、完整应用 build 或桌面验证。
