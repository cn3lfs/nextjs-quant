# W3 开发交付（2026-09-13）

执行依据为 [任务书](../tasks/w3-counterfactual-discipline.md) §7 > §6 > 前文。管理者尚未人工验收，未 commit/push/打包，不修改用户维护的 MCP 文件或文案。

## 交付

- `src/lib/discipline-counterfactual.ts`：纯重放函数、双自校验、20 点完整网格、净值与逐回合/逐成交对照。
- `src/server/discipline-{source,worker,service}.ts`、`src/server/api/root.ts`、`scripts/build-runtime.mjs`：只读 GBBQ/行情证据、显式覆盖检查、后台进度/取消/超时、完整导出、V5 一次记账。
- `src/components/discipline-{container,results}.tsx`、`trade-review-container.tsx`：交易复盘入口、完整固定顺序表格、A/B 和排除计数、新固定文案、加载/失败/重试/导出。
- `src/lib/research-usage.ts`、`src/components/research-usage-panel.tsx`：新增纪律反事实使用类型与展示；复用原存储，无迁移。
- `tests/discipline-counterfactual.test.ts`、`tests/discipline-service.test.ts`：纯函数、页面渲染、GBBQ 接入、worker 生命周期与真实隔离 V5 存储测试。
- `docs/decisions.md`、`docs/next-plan.md`：口径取舍及当前交付状态。

## 真实数据核对

以 `readonly: true, fileMustExist: true` 打开现有隔离库 `.test-data/w2-import/quant.sqlite`，不调用应用迁移；通达信 `E:/new_tdx64` 只读。未手动设置 QUANT_DATA_DIR，Vitest 沿用仓库自带隔离 setup。

| 自校验                              | 有效已平仓回合 | 净利润合计（元） | 结果 |
| ----------------------------------- | -------------: | ---------------: | ---- |
| A：不排除、不施加规则               |            784 |       -230837.38 | 对齐 |
| B：跨除权退出规则与统计、现金流保留 |            774 |       -197422.44 | 对齐 |

GBBQ 全文件最大事件日期 2026-09-18，账户末笔成交日 2026-04-16；已知跨除权 10 回合，有效回合除权未知 0。原历史另有 1 个收益无效回合，不算进 784/774。

真实 20 点均产出，未挑最优。采用期初现金 126200 元、实验佣金 3 BP/最低 5 元、股票卖出税 5 BP、开盘原价成交。此次 20 点均无资金不足未发生回合；资金不足分支另用可手算用例验证。A/B 是回合利润合计，不是账户净值差。

## 检查

- 新增相关测试 22 项通过；覆盖第三日收盘触发/第四日开盘成交、防未来价格与费用影响、停牌顺延、止损后作废、加仓手算、整手资金约束、跨除权现金保留、逆回购、覆盖拒绝、20 点展示、V5 记账及失败/取消/超时/结果过期。
- 阈值相等的负控曾复现 `9.5/10-1 < -0.05` 的浮点误触发；改为金额比较并忽略数个 ulp 后通过，未放宽有经济意义的阈值。
- 最终全量 `pnpm test`：1929 通过、6 既有跳过；288 文件通过、5 既有文件跳过，包含阈值边界与独立现金自校验的最终修改。
- 最终 `pnpm typecheck`、`pnpm build`（含 runtime 重建）、全部改动文件 Prettier 与 `git diff --check` 通过。
- 重建后的真实 worker 独立运行同一隔离账户，20 点全部返回、A/B 分别通过、GBBQ 覆盖通过，行情文件读取警告 0；并非只有纯函数或 mock worker 验证。本轮三个临时核实脚本已清理，之前的基线阻断核实脚本及记录保留。
- 页面已做静态渲染断言，尚未做真实浏览器交互验收。

## 冲突与未处理风险

旧任务书的网格/规则、跨除权整体删除、单基线及旧措辞按 §7/§6 覆盖，无新管理者裁定阻断。完整最大回撤受真实 TWR 估值缺口影响，20 点均留空并带原因，未用分段值或假估值凑齐。

期初资金只是反推下界；原始现金/股份证据缺口仍存在。新增止损费用是固定实验假设，原成交缩量沿同比费用并允许理论碎股；缺行情无法区分停牌与文件缺失；未建模涨跌停成交受阻、历史费率和完整公司行动结算。这些限制使本批只能用于事后对照，不能作为可交易结论或可信策略业绩。
