# W10 同日内资金顺序交付

## 实现边界

- 新增 `src/lib/trade-review-intraday-order.ts`：已排序事件按日期分组，仅移动逆回购有限正 `netAmount`、现金管理有限正 `amount`，移到该日首笔流出之前；其余记录保持原相对顺序。流出识别复用净值既有的实际金额/费用回退语义，不猜回款方向。
- `src/lib/trade-review-nav.ts` 在期初余额推断之后执行该步骤，通过现有 warnings 输出「同日内逆回购与现金管理流入已提前」，现有净值诊断 UI 和 JSON 均可看到；不改原始时间或输入数组。日末沿原始加法顺序累计，保证现金序列连浮点尾数也不变。
- `trade-review.ts` 未修改。其排序是日期→时间→原始行号→输入索引；净值既有排序是日期→有成交余额时优先原始行号→已知时间→输入索引。W10 只在净值原排序后追加步骤。
- 新增 `tests/w10-intraday-order.test.ts`；更新 W9 真实测试的最低现金目标，其他 W9 分类/数量/金额断言保留。同步 `invariants.md`、`decisions.md`、`next-plan.md`。

## 四项真实数据验收

来源为 `.test-data/statements-v2/lishi20-26.xls`（all）与 `duizhang20-26.xls`（cashFlowsOnly），期初现金 0。日期轴为组合导入后所有事件日期的去重升序，共 701 日；没有虚构日历或行情，`bars={}`，此验收只用于现金、回合和流水，不用于宣称 TWR 完整可用。W9 真实测试另外经过内存 SQLite 导入/去重/读取路径，结果相同。

| 指标           |                   排序前 |                         排序后 |
| -------------- | -----------------------: | -----------------------------: |
| 已平仓回合     |                      785 |                            785 |
| 胜率           |       50.89285714285714% |             50.89285714285714% |
| 盈亏比         |       0.8912012172774283 |             0.8912012172774283 |
| 日末现金序列   |                   701 日 | 701 日逐项精确相等，最大差异 0 |
| 最低现金       | -126036.55（2024-01-12） |         -9007.25（2021-08-25） |
| transferIn     |       64 条，+1017222.79 |             64 条，+1017222.79 |
| transferOut    |        30 条，-788997.34 |              30 条，-788997.34 |
| cashManagement |           48 条，+114.13 |                 48 条，+114.13 |

完整回合输出前后相等，不只比较三个汇总数。785 个回合中 1 个不参与胜率，既有有效统计分母仍为 784，399 胜/385 负。W8 修改前非逆回购完整输出 SHA-256 护栏保持原值。

原文件 SHA-256：

- lishi：`50f36c587e751729f1f6bd57ca806c67611095efa17f19918142e36f61b22198`
- duizhang：`2edc4101362f181481df5aafbaa009782fbd95ff3a0bbd43771bc960e52f69ea`

## 任务书差异与未处理风险

1. W9 补充建议提前普通证券卖出，与 W10 冲突；按 W10 保持普通证券相对顺序，不追求把最低现金变正。
2. 对账单余额列的 710 日/3 个负日末/-64892.91，不是组合导入复盘的口径。指定组合实测 701 个有事件日期、67 个负日末，最低日末 -181.03 元；排序前后完全不变。对账单自己的 3 天异常、本组合残余负现金及来源间差异均未修复。
3. -64892.91 是预期量级，不是排序目标；实测 -9007.25，改善 117029.30 元。不能据此推定所有日内负数均已解释。
4. 三笔手算未指定期初现金，本测试明确设 50，原路径 -50→50→0，新路径 150→50→0；零期初且净流出 50 不可能全程非负。
5. 真实 2775 笔成交没有柜台现金余额。其他数据源若需重排的日期含成交余额锚点，直接采用旧绝对余额可能改变日末，本批明确报错要求核对；没有擅自平移/丢弃柜台证据。原本不需要重排的余额路径保持。
6. W9 旧最低现金断言与本批修复目标冲突，更新到新路径实测值；旧路径 -126036.55 仍由 W10 的恒等排序负对照精确保护，不删用例、不新增生产开关。

货币基金在途资产未估值，未恢复真实盘中时序；本金未知、负净值与执行损耗 `boundaries` 守卫未放宽，重跑执行损耗留管理者执行。本批无新增依赖、迁移、提交、推送或打包；未修改 `src/server/mcp.ts` 或相关 UI。

## 验证

- 专项：启用 `W8_REAL_DATA=1`、`W9_REAL_DATA=1`、`W10_REAL_DATA=1`，四个文件 123 项全部通过；包括指定手算、双顺序逐项配对/成本、跨日、输入不变、金额拒绝、余额锚点拒绝、显式说明及真实数据。
- `pnpm typecheck`：通过。初次新测试误传简化行情类型，已改为仅传回合所需的 fills/cashFlows。
- 最终 `pnpm test`（同样启用三个真实数据开关）：294 个文件通过、5 个文件跳过；2016 项通过、6 项跳过，耗时 210.47 秒，退出码 0。
- `pnpm build`：通过，包含 runtime worker 重建，12/12 静态页面生成完成；未执行桌面打包或发布。
- 八个改动文件的 Prettier 检查与 `git diff --check`：通过。

测试未手设 `QUANT_DATA_DIR`，沿用 `tests/setup-data-dir.ts` 自动隔离；真实源文件只读。构建单独指定仓库内 `.test-data/w10-build-data` 为隔离路径，实测未产生该目录。临时日志固定为 `.test-data/w10-tests.log`、`w10-focused.log`、`w10-build.log`，本轮核对后删除；异常退出时可由管理者删除这三个固定文件，不建立长期档案库。
