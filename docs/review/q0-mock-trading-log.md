> T5 当前处置（2026-09-10）：**关闭**。买入和对账已有实测；卖出仍未完成且需下一可卖日及委托授权，不是几十秒核对，冻结可选模拟盘不继续实操；保留未完成事实，不重复买入。 本文其余签收栏及续做措辞保留为历史记录，不再另挂待办；当前用户操作见 [todo.md](todo.md)。

# Q0 同花顺模拟盘实操记录

执行日期：2026-09-10。用户已授权模拟开户与下单。本次仅使用既有 TypeScript MockTradingAdapter；未连接真实券商。

## 实际操作

隔离数据目录：`D:\github\nextjs-quant\.test-data\q0\data`。生产凭证文件不存在，因此未复用已有生产账户。用户名、资金账号均按凭证处理，不在本文披露；开户前已通过既有 vault 写入 DPAPI。

| 顺序 | 实际请求（凭证脱敏） | 实际观测 |
|---|---|---|
| 1 | GET http://trade.10jqka.com.cn:8088/pt_add_user，datatype=json，usrname=[DPAPI身份]，yybid=997376 | 既有适配器通过成功码校验，errormsg 通过数字资金账号校验；资金账号已保存至 DPAPI |
| 2 | GET 同主机 /pt_qry_stkaccount_dklc，datatype=json，usrid=[资金账号]，yybid=997376 | 成功码校验通过，但 result 未通过非空股东数组校验；实际异常：`股东账号响应不符合契约，请人工核对` |

原始响应体未被既有适配器返回或保存，因此不能披露 result 的实际字段，更不能声称已定位具体字段差异。api-spec.md 约定 result 为数组、gddm 为非空字符串、scdm 为字符串 1/2；本次只能确认响应不满足既有这组校验。未为补录响应而重复请求。

## 后续步骤与对账

- 买入：未执行。账户仍为 creating，未达到可下单条件。
- 持仓、资金、当日成交：未执行，无 ready 账户。
- 卖出：未执行。即便买入成交，也必须遵守既有 T+1 可卖数量校验，不以同日往返为由绕过。
- 本地账本：没有写入交易；远程持仓未知，因此数量、成本、可卖数量与资金差异均为**不可核对**，不是零差异。
- 不重试开户/股东查询，不放宽契约，不覆盖本地账本。需人工核对该 DPAPI 身份的远程开户状态后才能继续 Q0。

Q0 B 层未完成。同花顺问财提供模拟炒股服务。

## Q0 限定重试（2026-09-10 09:53:51 +08:00）

复用上述隔离目录已有 DPAPI 身份，仅请求一次 `/pt_qry_stkaccount_dklc`，未调用开户接口。
HTTP **200**，`errorcode=0`、`errormsg=""`；`result` 与 `list` 各含 5 项。
实际结构（账号均脱敏）：

```json
{"usrid":"[REDACTED]","gdzh":"[REDACTED]","scdm":"2","mflg":"1","notice":""}
```

五项 `scdm` 依次为 `9`、`8`、`2`、`1`、`:`。
契约要求的 `gddm` 在所有项缺失（`result.0.gddm` 至 `result.4.gddm`: `invalid_type`）；
`result.0.scdm`、`result.1.scdm`、`result.4.scdm` 为 `invalid_enum_value`。
这是字段名与市场枚举不一致，不是空数组或明确空股东账号；没有等待重查，也没有把 `gdzh` 擅自映射为 `gddm`。

证据 `fetchedAt=1789005231851`，脱敏文本 `payloadHash=ca564bfb5e432d3dc17345c93ebcc1a61fded0caa0b2f5b027a28b01416cf967`。
完整脱敏响应已保存于该隔离数据目录的既有对象存储，可在交易日志页面「查看最近20次脱敏响应（本地）」查看。
哈希对应脱敏原始文本，不对应含凭证的原文。未变更数据库版本。

结论：**远程账户状态不可核对**。账户仍为 creating，未执行买入、持仓/资金/成交查询或卖出；
本地账本未新增交易，所有对账差异仍为不可核对。需管理者核实第三方新版契约后决定是否授权适配；本次不放宽校验。

同花顺问财提供模拟炒股服务。

## 中断后接续核验（2026-09-10）

本次以 SQLite 只读连接核对隔离库 `records/mock-diagnostics`：已有且仅有上述一次
股东查询证据，HTTP 状态、fetchedAt、payloadHash、脱敏原始响应及 8 项字段校验失败
均与限定重试记录一致。哈希按既有 EvidenceEnvelope 规则计算：
`SHA256(JSON.stringify(rawBody))`，其中 rawBody 是脱敏后的完整文本。
既有实现已在交易日志页面提供最近 20 次响应的本地查看入口。

因此本次没有再次开户、再次查询或等待重查。限定重试已经完成；Q0 交易闭环仍未完成。
需要管理者核实并决定是否接受新版 `gdzh` 字段和市场枚举契约，之后才能继续远程账户核对。

## Q0 契约适配后实操（2026-09-10 10:21–10:23 +08:00）

按 Q2a 补充裁定复用 `.test-data/q0/data` 既有 DPAPI 身份，不重复开户。
股东查询一次成功，兼容 `gddm` / `gdzh`，同时出现但不一致时拒绝；保留所有市场代码。
界面「文档契约与实测契约差异」列出两套字段及处理，最近 20 次脱敏响应仍可查。

| 步骤 | 实测结果 |
|---|---|
| 股东查询 | HTTP 200，账号 ready；原样保留 `9/8/2/1/:` |
| 买入 | `sh601398`，100 股，8.07 元；市场 2 使用文档上海契约。HTTP 200、errorcode=0，result/list 为含 entrust_no 的数组，仅确认委托受理 |
| 持仓 | HTTP 200、errorcode=-1；`requested api web_qy_stock is not exists` |
| 资金 | HTTP 200、errorcode=0；`result={}`、`list=false`，缺失 `result.list`，不可核对 |
| 当日成交 | HTTP 200、errorcode=-1；`用户不存在1`，返回 result/list 下 ERRORCODE/ERRORMSG，而非 ret.item |
| 卖出 | 调用既有卖出流程，其可卖数量前置查询再次得到上述接口不存在错误；未发出卖出委托。没有绕过 T+1 校验 |

买入价格及当日上下限来自东方财富 `push2.eastmoney.com/api/qt/stock/get`，
证券 `f57=601398`，`f43=807/f51=880/f52=720`，`f59=2`，`f86=1789006938`。
即最新价 8.07、涨停 8.80、跌停 7.20；未从历史收盘推测限价。
先前腾讯报价读取 TLS 失败，未使用该来源数值。

请求证据 fetchedAt / 脱敏 payloadHash：

- 股东：1789006869714 / ca564bfb5e432d3dc17345c93ebcc1a61fded0caa0b2f5b027a28b01416cf967
- 买入：1789006984284 / f9b55c64b2fa03bb8778dc617a7a155f882eedbed3b6254ac03fcc81d9bb86c3
- 持仓：1789006984726 / 9b6acc29f47ed2b943d23649908e89dba4707cc10ee9a3b6f1414ca12500f4a4
- 资金：1789006985197 / b3e0020c1d71555d7e2aeab75a30640840f66ea4ed1512922cb018ec637a07f4
- 成交：1789006985675 / 605ddd94028cf6640f201712044cdfff030ecd117e315dd890b7bd5ec5cd01e0
- 卖出前置持仓：1789006986342 / 9b6acc29f47ed2b943d23649908e89dba4707cc10ee9a3b6f1414ca12500f4a4

**闭环未完成**。无已确认成交，不把受理写为本地成交；本地交易仍未新增。
持股、可卖、成本、资金和成交差异均为「不可核对」，不是零差异。
`9/8/:` 没有可推导市场含义的证据，仍标「市场代码含义未确认」。
停止远程尝试，不猜新接口、不更换身份、不重复买入。
实操入口 `tests/q0-contract-live.ts trade` 有本地一次性尝试标记，阻止重跑下单。

同花顺问财提供模拟炒股服务。

## 本轮最终状态（2026-09-10 10:58 +08:00；以下状态取代早前未核对结论）

已完成**买入成交确认 → 持仓/资金/成交查询 → 隔离本地账本录入 → 对账**。卖出流程已调用，因远程可卖为 0 被 T+1 前置门禁阻止，未发出卖出委托；**完整往返闭环仍未完成**，需下一可卖交易日继续，不能重复买入。

根因不是远程账户不存在：技能 `scripts/stock_query.py` 与同目录 `references/api-spec.md` 有三个请求契约差异。只读请求按脚本核验后确认：

| 项目 | 文档 / 旧请求 | 已实测契约 |
|---|---|---|
| 持仓 | `/pt_web_qy_stock`；data[] | `/pt_web_qry_stock`；result[]（文档少一个 r） |
| 资金 | usid；result.list[].dje | usrid；list[].djje |
| 当日成交 | usrid；ret.item[].cjg/cje | usrname（资金账号）；result[].cjjg/cjje/cjsl |

TS 适配器显式接受两套响应格式，采用脚本有出处的请求路径/参数；所有实际请求保留脱敏原始响应、HTTP 状态、fetchedAt、payloadHash 和失败字段。没有执行 Python 脚本、猜新接口、换身份或重复买入。市场枚举 9/8/: 仍显示「市场代码含义未确认」。

实测工商银行 `sh601398` 于 **10:23:03 买入 100 股 × 8.07 = 807 元**，费用 **0.26 元**（sxf 0.24 + ghf 0.02），资金发生数 807.26 元。持仓 gpsl=0、djsl=100、kysl=0，结合成交100股、gpye=100及市值可核实总持有100股：本套响应使用 gpsl+djsl 计总数量，kysl 才是可卖数量，不把冻结股份视为可卖。

隔离库 `.test-data/q0/data` 已录入唯一固定 ID 的确认买入，重复记录同 ID 幂等。普通 recordLocalTrade 首次尝试因本地已完成日线日历截止9月9日拒绝，未写入；实操用例随后使用「本地已有日期 + 已核实当日远程成交日期」作为显式记账上下文，未改生产或隔离设置中的日历，也未绕过 T+1。账本仍用既有实验费用，没有改成本规则或自动覆盖远程数据。

| 对账项 | 本地 | 远程 | 差异 / 解释 |
|---|---:|---:|---|
| 持股 | 100 | 100 | 0 |
| 可卖 | 0 | 0 | 0；本日不能完成卖出 |
| 买入费用 | 5.4035 | 0.26 | +5.1435；本地最低佣金5 + 滑点0.4035，远程实际0.26 |
| 每股成本 | 8.124035 | 返回8.073 | +0.051035；远程按实际金额推得8.0726，返回成本另有三位舍入 |
| 买入现金支出 | 812.4035 | 807.26 | +5.1435；本地仅有交易现金流，没有初始资金账户 |
| 可用资金 | 不存在对应资金账户，不能作余额对账 | 199192.74 | 不报零差异 |
| 远程 zjye / 冻结资金 | 不适用 | 200000 / 0 | zjye 与可用资金原样保留，不猜测其清算语义 |

本地估值使用9月9日收盘8.00，远程盘中价格变化，浮盈不是同一时点，不能直接报策略或账户盈亏差。

完整脱敏证据：[q0-reconciliation.json](q2b-review/q0-reconciliation.json)。查询成功不删除诊断历史。卖出结果为 `远程 T+1 可卖数量不足`；远程卖出委托请求数为 **0**。

同花顺问财提供模拟炒股服务。
