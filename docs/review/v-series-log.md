# V 系列执行日志

管理者：Claude。执行者：codex（`codex exec -s workspace-write`，后台运行）。
立项依据：[个人量化软件调研](../personal-quant-survey.md) 的差距对照。
排期与边界：[next-plan §7](../next-plan.md)。

顺序：V1 → V2 → V3 → V4 → V5，串行。

## 每批的固定流程

1. codex 按任务书实现（后台运行，避开 10 分钟命令超时）
2. 管理者独立跑 `pnpm typecheck` / `pnpm test` / 改动文件的 prettier
3. 管理者审 diff：口径、缺失语义、是否越界、是否动了不该动的
4. 通过则本地提交；不通过则带着具体证据打回
5. 结果记入本文件

沿用 U 系列踩过的坑：跑测试不设 `QUANT_DATA_DIR`；`codex exec` 后台运行；
只保证本批改动文件通过 prettier，不批量修历史文件。

## 授权边界（本系列全程有效）

本地提交、**不推送**；不打包、不接触生产库、不外发、不新增依赖；
除非任务书明确允许，不新增数据库迁移。

## 批次记录

（按批次追加）
### V1 试验计数与紧缩夏普 —— 已交付并提交

执行者提了三条冲突，**三条都成立**，任务书是错的一方：

| 冲突 | 事实 | 处置 |
| --- | --- | --- |
| 页面入口 | 任务书写 `backtest-view.tsx`；实际滚动结果、档案与导出都在 `walk-forward-panel.tsx`，前者只挂载后者 | 新建纯展示 `multiple-testing-panel.tsx` 挂到实际组件，不碰回测表单 |
| `sharpes: number[]` | 合法空仓候选的 equity 是常数，U1 夏普按规则返回 null，类型给不出数 | 改 `(number \| null)[]` + 同序原因数组；任一候选不可估则方差与 DSR 整体留空，**不缩小 N** |
| 相关性方向 | 任务书断言"候选相关 → 方差偏小 → DSR 偏乐观" | **只对了一半**：相关性压低方差使门槛偏低（偏乐观），但原始 N 同时高于有效独立试验数使门槛偏高（偏保守），净方向不确定。改为如实说明未校正独立性、方向不保证 |

第三条是本批最有价值的一次打回。我在任务书里把一个方向不定的偏差写成了单向，
执行者按论文（Eq. 2 与 Appendix 3 用的是**独立**试验数）纠正并留下依据，
没有顺着任务书写一句读起来更安心但不成立的话。

另外两处执行者自行判断且判断正确：`multipleTesting` 设为可选字段
（旧档案按原类型直读、本批禁止迁移，旧结果显示"未记录"而不是补算伪造），
`yearlyDays` 作为可选入参（roadmap §2 的多品种约束：A 股假设不内联成常量）。

验收结果：

- `pnpm typecheck` 通过；`pnpm test` **279 文件 / 1822 测试通过**，5 文件 / 6 跳过（既有环境跳过）
- `pnpm format:check` 全量通过
- `walkForward` 既有计算路径**一行未改**：diff 中 folds 循环与 summary 表达式零改动，
  新增调用插在 `returns` 之后，`backtest` 无副作用。另加一条 summary 七字段
  硬编码特征化断言防未来漂移
- 日夏普复用 `dailyPerformance({ yearlyDays: 1 }).sharpeWbt`，**没有第二份夏普实现**；
  测试里有一条同源交叉断言钉住这件事
- 紧缩门槛用 `-(1-γ)Φ⁻¹(1/N) - γΦ⁻¹(1/(Ne))`，靠正态对称性避开大 N 时 `1 - 1/N` 舍入为 1
- 页面三档结论文案把出处与局限焊死，任一输入为 null 时显示原因原文；
  「N 是下界」同时出现在 `assumptions` 与页面
- `package.json` / `pnpm-lock.yaml` 未改，无新增迁移

**未做（不在本批范围）**：真实浏览器验收、V2 的 PBO。
