# W3 基线核实阻断（2026-09-13）

已全文阅读任务书，以 §6 为准，并核对 W2 更正节。实现前的真实数据核实发现：§6 的「跨除权回合整体排除」与「网格基线仍等于 −230837 元」无法同时满足，依用户要求停止实现。

## 方法与结果

使用 `better-sqlite3` 的 `readonly: true, fileMustExist: true` 打开既有隔离库 `.test-data/w2-import/quant.sqlite`，不调用应用数据库初始化或迁移，不设置 QUANT_DATA_DIR。通过 `DeliveryStore` 读取 real 账户，复用 `reviewTrades` 移动加权回合计算。只读解析 `E:/new_tdx64/T0002/hq_cache/gbbq`，传入类别 1/11/12 的证券与事件日期；按既有规则匹配 `(openingDate, closingDate]`。GBBQ 全文件最大事件日期为 2026-09-18，覆盖本次已平仓回合结束日期。

| 口径                         | 回合数 | 净利润合计（元） |
| ---------------------------- | -----: | ---------------: |
| W2 原有效已平仓回合          |    784 |       -230837.38 |
| GBBQ 确认跨除权，需整体排除  |     10 |        -33414.94 |
| 除权状态未知，单独排除       |      0 |             0.00 |
| 排除后可参与对照的原历史回合 |    774 |       -197422.44 |

差额等于被排除回合的净利润，非账户净值口径或浮点误差。举例：sz300398，2024-11-12 开仓、2025-01-08 平仓，区间含 2024-12-27 事件，原回合净利润 -32104.90 元。

以上是原历史回合过滤后的会计合计，尚未施加资金约束重放，不宣称它就是最终反事实现金曲线。删除整个回合也会改变后续可用资金；需要在基线契约中明确处理。

## 待管理者裁定

建议将两个验收分开：完整历史重放核对 -230837.38 元；20 点对照使用共同排除集合，并独立核对其可比基线。另一种口径是除权回合仅退出规则与统计、现金流原样保留，但这与当前「整体排除」不同，需要明确裁定。执行者未自行采用任一方案。

另外，现有 `TradeReviewInput.exRightsEvents` 仅含事件列表，没有覆盖截止日字段；`reviewTrades` 收到列表即对未匹配事件的已知开仓回合返回 false。正式实现必须另外传递/检查 GBBQ 覆盖信息，避免覆盖不足被误判为 false。

W2 更正中的买入次数及首笔金额汇总支持研究加仓限制，但没有直接检验每次加仓时的浮亏，不能单凭这些汇总确认「亏损后加仓」或因果主因；这不妨碍将加仓限制作为反事实研究问题。

## 本轮交付与检查

仅新增本核实记录；临时只读复现脚本位于 gitignore 的 `.test-data/w3-baseline-audit.ts`，可在管理者核实后删除。脚本成功执行，退出码 0。未实现纯函数、页面、导出或 V5 记账，未新增依赖/迁移，未 commit/push。由于命中任务要求的停止条件，未运行应用测试与 typecheck；对新增文档及临时脚本执行 prettier，并检查 git diff。
