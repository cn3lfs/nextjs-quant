# W9 现金管理交付说明

新增 `cashManagement` 现金流类别，限定于对账单 `cashFlowsOnly` 的完整摘要结构。发生金额符号决定流入/流出，未知方向留 unresolved 并计数；23 条操作方向冲突存入逐笔 warnings 与批次 diagnostics。没有修改交易匹配、净入金、期初现金、迁移或依赖。

## 真实数据

只读 `.test-data/statements-v2/lishi20-26.xls` 与 `duizhang20-26.xls`，通过 `commitDeliveryImport` 导入 `:memory:` SQLite（成交 all、对账单 cashFlowsOnly），用 `DeliveryStore` 读回后调用现有 `reviewTrades` / `reviewTradeNav`。数据库在 finally 关闭，不使用生产库。未手设 QUANT_DATA_DIR；Vitest 沿用仓库 setup 自动隔离。

期初现金明确为 0；日期取全部成交与现金事件日期并集，`bars: {}`。现金重放不依赖行情，因此可以核实最低现金；本次没有测量 TWR、持仓市值或净资产绩效，不将缺失行情补造为价格。

| 硬验收项    | 实测                                                       |
| ----------- | ---------------------------------------------------------- |
| 现金管理    | 48 条，+114.13 元；未知方向 0 条，操作冲突 23 条           |
| transferIn  | 64 条，+1,017,222.79 元                                    |
| transferOut | 30 条，-788,997.34 元；真实净入金仍为 +228,225.45 元       |
| 最低现金    | -126,036.55 元，2024-01-12                                 |
| 回合统计    | 785 个；胜率 50.89285714285714%；盈亏比 0.8912012172774283 |

负对照为同一读回数据中去掉 cashManagement：最低现金 -126,150.68 元，日期同为 2024-01-12。胜率分母仍为 784（399 胜、385 负），另有 1 个排除回合。三项统计与补入前一致。

## 任务书冲突与处置

1. 真实摘要是 `[007864]申购扣款[金额]元，8214申购扣款` / `赎回返款[金额]元，8216赎回返款`，前有数字流水号。规则严格接受这些完整结构及任务书给出的简式，不按“申购”关键词兜底。
2. 370409 实际有两条（新股中签扣款 -1000 元、申购中签转非流通 0 元），不是本批货币基金；保持 subscription。
3. 48 条全部在 2021 年，补入后仅将 2024 年最低现金抬高 114.13 元，未证实诊断文档关于主要缺口来源的结论。按任务书允许的负现金结果如实报告，停止在本批分类边界。
4. W5b 既有测试把同结构申购扣款留 unresolved，与 W9 授权相反；只更新该记录的分类与留痕断言，其余拒绝断言和旧 all 完整快照保留。

## 验证与风险

- 定向导入测试：W5/W9 共 40 项通过（启用 W9 真实文件验收）。
- `pnpm typecheck`：通过。
- `pnpm test`：启用 `W8_REAL_DATA=1`、`W9_REAL_DATA=1`；293 文件通过、5 跳过，2005 项通过、6 跳过，无失败。
- `pnpm build`：通过，包含 runtime 重建、TypeScript 与 12/12 静态页面生成。构建单独指定 `.test-data/w9-build-validation` 为隔离数据路径，结束后核实该目录未产生；测试命令没有设置 QUANT_DATA_DIR。
- 六个改动文件均已运行 Prettier，格式检查及 `git diff --check` 通过。

新测试先在修改前观察到分类缺失导致失败；覆盖结构区分、正负方向、缺失/非有限/零发生额与禁止成交金额兜底、未知摘要、代码冲突、持久留痕、重复导入和内部流不剔除出 TWR。真实数据用例通过 `W9_REAL_DATA=1` 显式启用，缺文件会失败，不静默略过。

剩余风险：104 条 unresolved 仍未处理；其他缺失来源及跨日归属未继续诊断；货币基金在途资产与估值未建模。旧导入批次受文件幂等保护，重新导入同一文件不会自动补入，需管理者核对并撤销旧批次后重新导入。生产库、浏览器和已打包桌面版未操作，未 commit / push / 打包。
