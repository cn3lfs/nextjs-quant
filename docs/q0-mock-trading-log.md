# Q0 同花顺模拟盘实操记录

执行日期：2026-09-10。用户已授权模拟开户与下单。本次仅使用既有 TypeScript MockTradingAdapter；未连接真实券商。

## 实际操作

隔离数据目录：`D:\github\nextjs-quant\.test-data\q0\data`。生产凭证文件不存在，因此未复用已有生产账户。用户名、资金账号均按凭证处理，不在本文披露；开户前已通过既有 vault 写入 DPAPI。

| 顺序 | 实际请求（凭证脱敏） | 实际观测 |
|---|---|---|
| 1 | GET http://trade.10jqka.com.cn:8088/pt_add_user，datatype=json，usrname=[DPAPI身份]，yybid=997376 | 既有适配器通过成功码校验，errormsg 通过数字资金账号校验；资金账号已保存至 DPAPI |
| 2 | GET 同主机 /pt_qry_stkaccount_dklc，datatype=json，usrid=[资金账号]，yybid=997376 | 成功码校验通过，但 result 未通过非空股东数组校验；实际异常：`股东账号响应不符合契约，请人工核对` |

原始响应体未被既有适配器返回或保存，因此不能披露 result 的实际字段，更不能声称已定位具体字段差异。api-spec.md 约定 result 为数组、gddm 为非空字符串、scdm 为字符串 1/2；本次只能确认响应不满足既有这组校验。未为补录响应而重复请求。

## 后续步骤与对账

- 买入：未执行。账户仍为 creating，未达到可下单条件。
- 持仓、资金、当日成交：未执行，无 ready 账户。
- 卖出：未执行。即便买入成交，也必须遵守既有 T+1 可卖数量校验，不以同日往返为由绕过。
- 本地账本：没有写入交易；远程持仓未知，因此数量、成本、可卖数量与资金差异均为**不可核对**，不是零差异。
- 不重试开户/股东查询，不放宽契约，不覆盖本地账本。需人工核对该 DPAPI 身份的远程开户状态后才能继续 Q0。

Q0 B 层未完成。同花顺问财提供模拟炒股服务。
